# 設計決定録（Design Decisions）

このプロジェクトの設計判断を記録する。決定を変更する場合も行は消さず、決定・理由を更新して経緯を理由欄に残す。

## 決定事項

| # | 論点 | 決定 | 理由 |
| --- | --- | --- | --- |
| 1 | リポジトリ構成 | モノレポ。`billing-mcp/`（売り手）と `agent-app/`（買い手）をディレクトリで分離。ルートは記録・共通設定のみ | 課金サービスと利用側を独立にデプロイ・検証できる。ops-agent 同様、workspace 統合はせずアプリ別ツールチェーンで運用 |
| 2 | billing-mcp の機能 | html-creator-mcp-apps を踏襲（generate-html ツール + プレビュー UI）。コードは流用せず最新 SDK で書き直す | 踏襲元は SDK 1.29 + 自前セッション管理など旧プロトコル前提の箇所が多く、部分改修より作り直しが安い |
| 3 | MCP 仕様の線 | `@modelcontextprotocol/sdk` 1.30 系（プロトコル 2025-11-25）+ `ext-apps` 1.7 系（MCP Apps 2026-01-26 正式版）。最新リビジョン 2026-07-28 には今回は追随しない | MCP Apps SDK（ext-apps）が v1 系専用で、2026-07-28 実装の SDK v2（`@modelcontextprotocol/server` 等）と併用不可（2026-08-30 時点。移植 PR 未マージ、適合性違反 issue 10件 open）。UI 配信を優先し、ext-apps の v2 対応後にアップグレード（U5） |
| 4 | トランスポート | Streamable HTTP。Lambda Function URL の POST `/mcp` で待受（ローカル開発も同じパス） | 当初は AgentCore Runtime の MCP ホスティング契約（コンテナ 0.0.0.0:8000 の /mcp）に合わせていた。決定19 で売り手を Lambda へ移したため、待受はコンテナのポートではなく Function URL 上のパスになった（2026-08-30 更新） |
| 5 | billing-mcp のデプロイ | CDK（Amplify 不使用）。ops-agent 方式: 関数ベーススタック、parameter.ts（gitignore + sample コミット）、jest + @swc/jest、cdk.json は tsx 実行。デプロイ先は Lambda（zip・arm64）+ Function URL | ユーザー要件（MCP 部分を切り出して CDK デプロイ）。参考元の規約に合わせて学習コストを下げる。デプロイ先は当初 AgentCore Runtime だったが決定19 で Lambda へ変更（2026-08-30 更新） |
| 6 | AgentCore Runtime の構成方法 | 不採用。当初は `aws-cdk-lib/aws-bedrockagentcore` の安定版 L2（Runtime + AgentRuntimeArtifact.fromAsset、linux/arm64）を使う予定だった | 着工前調査で L2 の実在（aws-cdk-lib 2.267.0 に environmentVariables / ProtocolType / authorizerConfiguration / fromAsset がすべて存在）、東京リージョンでの稼働、CDK bootstrap v32 の導入済みを確認していた。しかし決定19 で AgentCore Runtime 自体をやめたため本決定は失効。将来 AgentCore へ戻す場合はこの構成でよい（2026-08-30 更新） |
| 7 | x402 売り手実装 | `@x402/*` v2 系 SDK。facilitator は x402.org（Coinbase 運営・API キー不要） | x402 は v2 が現行仕様。旧 `x402-express` 等 v1 パッケージは deprecated。テストネットなら無料 facilitator で完結 |
| 8 | 決済ネットワーク | Base Sepolia（eip155:84532）+ テスト USDC（Circle Faucet で入手） | サンプルのため実マネーゼロで全フローを検証する。**追記（2026-09-07、セキュリティ点検）**: この選択の帰結として、テストネットでは x402 が経済的な抑止力を一切持たない。売上はテスト USDC（faucet で無料）で原価だけが実費の Bedrock ドルなので、「価格 ≥ 最悪原価」という x402 本来の歯止めは破れているのではなく成立せず、`PRICE` を上げても意味がない。攻撃者は何も破らずに faucet → 正規の支払い → `maxTokens` の使い切りで売り手に実費を負わせられる。売り手の支出を実際に縛るのは `reservedConcurrency`・関数タイムアウト・Bedrock のクォータという技術的な上限だけ。したがって検証が済んだら売り手のスタックを壊す（ユーザー決定。billing-mcp/README.md「後片付け」）。この歯止めが本当に効くのは mainnet で実資産を受け取る場合に限る |
| 9 | 買い手ウォレット | AgentCore Payments（2026-08-18 GA）。Coinbase コネクタ + ~~Quick Create~~ **MANUAL**（CDP キー持参）、PaymentSession の maxSpendAmount で**セッション単位の**支出上限（時限つき。累積の上限ではない） | AWS マネージドウォレットを試すこと自体が本サンプルの狙いに合致。テストネット対応・Runtime 非依存の公開 API であることを調査で確認済み。自前ウォレット（viem + 秘密鍵）案は不採用。**変更（2026-09-02）**: コネクタの作成方式を Quick Create から MANUAL へ変更（AWS コンソールの支払い画面が白画面のまま進まず3回失敗したため）。経緯と手順は決定27 を参照。**補足（2026-09-03、PR #2 レビュー）**: maxSpendAmount は1セッション（検証では60分・1.00 USD）の上限で、セッションを作り直せば新しい枠が切られる。累積の上限にはならない点は決定19 の「同時実行数は累積コストの上限にはならない」と同じ区別。1回あたりの上限は買い手コードの支払いポリシー（`PAYMENT_MAX_AMOUNT`）で別途担保する |
| 10 | 支払い主体と UI 経路 | ハイブリッド。agent-app の Agent（サーバー側）が支払って有料ツールを実行。ブラウザは ui:// リソースを無課金で直接取得して iframe 描画し、ツール結果は Realtime 経由で受領 | エージェントが自律的に支払う筋書きを保ちつつ、MCP Apps の UI はブラウザでしか描画できない制約と両立させる。ブラウザ側ウォレット案・Agent 全中継案は不採用。ブラウザ直接取得の成立性は着工前に実測で確認済み（AgentCore データプレーンは CORS 全開だった）。決定19 で Lambda へ移したため CORS は自前で設定する（2026-08-30 補足） **補足（2026-09-03、④着工）**: 「ブラウザが ui:// を直接取得」の実体は売り手 `/mcp` への `resources/read`（JSON-RPC）で、CORS は `server/src/app.ts` が自前で返す形で③時点で実装済み（クラウドからブラウザで実測するのは④）。また売り手の preview-view は `ontoolresult` 待ちのため、ブラウザは MCP Apps の**ホスト**（`AppBridge`）を実装しないと何も描画されない。描画経路の最終形は決定29 に記す |
| 11 | 課金の粒度 | 有料はツール呼び出しのみ。ui:// リソース取得・初期化等は無課金 | 決定10の前提。UI 取得に支払いを要求すると描画経路が破綻する **補足（2026-09-03、④着工）**: 無課金なのは `resources/read`（ui:// の取得）と `initialize`。④のブラウザ経路はこれに依存する（決定29） **補足（2026-09-03、Q&A）**: 「無課金だと中身を無料で取られないか」という問いに対する答えは「取られる中身がそこに無い」。ui:// が返すのは `preview-view.html` を `readFileSync` した静的な**空の表示器** 1 枚で（`billing-mcp-server.ts:59`）、生成 HTML は `tools/call` の結果としてしか出ず、その生成は `createPaymentWrapper` の upfront により決済確定後にしか起きない（同 `:25, :85`）。無認証で取られ得るのは表示器・ツール説明文・Lambda の実行費だけで、費用面の防護は決定19 のとおり。誤解を避けるため、構成図・README・本書の表記を「空の表示器の取得（生成物は含まない）」に揃えた |
| 12 | リージョン | 基本 ap-northeast-1（東京）。AgentCore Payments 一式のみ ap-southeast-1 に作成しクロスリージョン呼び出し | Payments は東京非対応。リスク: クロスリージョン利用は公式に禁止も保証もされていない。支障が出たら全リソースを ap-southeast-1 に集約する切替案を用意 |
| 13 | agent-app の実装 | AWS Blocks。Agent Building Block + AuthCognito 等、handson-aws-blocks を参考。雛形はスキャフォールド生成 | ユーザー要件 |
| 14 | 作業記録 | 本書（設計決定録）+ implementation-log.md（日付別記録）+ CLAUDE.md で更新義務化 | ユーザー要件（経緯を必ず残す）。ops-agent の運用を踏襲。Issue/PR 駆動は今回は採らない |
| 15 | ツールチェーン | mise（node 24 / pnpm 10）。pnpm の適用は billing-mcp のみ。agent-app はスキャフォールドが採用するパッケージマネージャ（npm 想定）に従う。billing-mcp サーバーは Biome + Vitest、CDK テストは jest + @swc/jest | 参照元2リポジトリの選定の折衷（CDK 側は ops-agent、サーバー側は html-creator の系譜）。当初は全体 pnpm としていたが、決定13「雛形はスキャフォールド生成・手書きしない」と衝突するためセルフレビューで適用範囲を限定（2026-08-30） |
| 16 | 進行順 | ①土台のみ → ②billing-mcp 単独で動作確認 → ③agent-app → ④結合（Agent が支払って UI が出る） | 一気通貫で作るより手戻りが小さい。各段階の区切りで implementation-log.md に記録 **追記（2026-09-03、④着工）**: ④の完了条件は「agent-app はローカル、売り手はクラウド」に限定し（決定28）、agent-app のクラウド deploy は **⑤** として後続させる。⑤の論点（支払い主体の二重化・selfSignUp とレート制限・`PAYMENT_*` の Lambda 配線と AppSetting 化）は決定28 参照。**追記（2026-09-04）**: ⑤の deploy 先と土台は決定33（Amplify Gen2） |
| 17 | x402 の MCP バインディング | `@x402/mcp`（2.24 系）のインプロトコル方式を採用。支払い要求はツール結果（isError + structuredContent の PaymentRequired）、支払い証明は tools/call の `_meta["x402/payment"]`、レシートは結果の `_meta["x402/payment-response"]`。HTTP 402 ステータス・ヘッダは使わない | 当初の理由は「AgentCore Runtime はレスポンスの 402 ステータスやカスタムヘッダを返せない（U2 の調査結果）ため HTTP レイヤの x402 は成立しない」だった。決定19 で Lambda へ移りこの制約は消えたが、インプロトコル方式は維持する。HTTP レイヤの x402 だと `/mcp` へのリクエスト全体が有料になり、ui:// リソースを無課金にする決定11 と衝突するため。`createPaymentWrapper` なら有料ツールと無償リソースを同居できる。MCP 公式の支払い拡張 SEP-2007 はまだ Draft のため今回は採らない（2026-08-30 更新） |
| 18 | フェーズ②の検証手段 | TDD は facilitator を偽 HTTP サーバーで差し替えて進め、仕上げに使い捨てウォレット（viem 鍵 + Circle Faucet のテスト USDC + x402.org facilitator）で実オンチェーン決済を 1 回流す。価格は 0.1 テスト USDC / 呼び出し（exact スキーム） | テストの安定性と実決済の証拠取りを両立。買い手本命（AgentCore Payments）の検証はフェーズ③以降。価格は当初 0.01 テスト USDC で仮決めしていたが、クラウドへの初回デプロイ時にユーザー判断で 0.1 へ引き上げた（2026-08-31 更新）。**検証完了（2026-08-31）**: クラウド（Lambda Function URL）に対して実オンチェーン決済を 2 回成功。売り手残高が 0.1 USDC ずつ増加（0.02 → 0.12 → 0.22）し、トランザクション確定を確認 **追記（2026-09-20）**: 価格は決定54 で呼び出しごとの動的な見積もりに改める。0.1 テスト USDC 固定はフェーズ②の検証手段としての値付けであり、以後は上限額の提示に置き換わる |
| 19 | 売り手（billing-mcp）のホスティング | AgentCore Runtime をやめ、Lambda（zip・arm64）+ Function URL（AuthType NONE = 無認証）に置く。無認証で公開するため、reserved concurrency と関数タイムアウトで瞬間的な流量に上限を掛ける（同時実行数は累積コストの上限にはならない。累積の上限が要るなら AWS Budgets 等を併用）。Lambda の制限（リクエスト 6MB / 実行 15分）を超えたくなった場合は ECS へ移す | AgentCore Runtime は匿名インバウンドを許さず、authorizer は IAM（SigV4）か JWT の二択しかない（aws-cdk-lib 2.267.0 の L2 にも CFN にも無認証は無い）。IAM で絞ると認可の主体が「支払い」ではなく「権限付与」になり、x402 で課金する意味が消える（「IAM で絞るなら x402 は不要では」というユーザーの指摘）。買い手側の AgentCore Payments（決定9）は Runtime 非依存と調査済みで、売り手を AgentCore に置く必然性は「AgentCore Runtime を試すこと」以外に無かった。x402 を唯一のゲートに戻すことを優先し、2026-08-30 に転換 |
| 20 | 検討して不採用にした売り手構成 | ①API Gateway（REST / HTTP）②AgentCore + Cognito Identity Pool のゲスト資格情報 ③AgentCore + 公開プロキシ（CloudFront + Lambda@Edge） | ①HTTP API は統合タイムアウト 30秒上限で最初から不可。REST API は統合タイムアウトを 29秒超へ引き上げ可能（Regional / Private のみ、Service Quotas の L-E5AE38E3。当アカウントは Adjustable: True / 現在値 29000ms）だが、`Idle connection timeout` 310秒は引き上げ不可で生成上限 570秒が通らず、引き上げにはアカウント・リージョン全体の API Gateway スロットル削減が伴い、さらにクォータ申請が `cdk deploy` の外に出て再現性を損なう。②未認証（ゲスト）ID の AWS 一時クレデンシャルで SigV4 署名すれば実質匿名公開は可能（Identity Pool ID は公開値でよい。※IAM の意味論からの推論で実地検証はしていない）だが、買い手に「ゲスト資格情報の取得と SigV4 署名」という AWS 固有の作法を強い、「事前の関係なしに HTTP と決済だけで買える」という x402 の売りが消える。③は前段が無認証の公開 Lambda になるため、後段の AgentCore が余剰になる |
| 21 | x402 の支払いフロー | `upfront`（settle をハンドラ実行前に実行）。`@x402/core` の `PaymentFlowName` は `authorization` / `upfront` / `escrow` の3種 | 既定の `authorization` は verify → ハンドラ → settle の順のため、署名は有効だが決済が通らない支払いを送りつけると Bedrock の生成コストだけ売り手が負担する。実装記録 2026-08-30 のとおり settle 失敗は 3回中 1回 実際に発生しており、無認証の公開エンドポイント（決定19）では机上の話ではなくなる。`upfront` なら決済確定後に生成するためこの穴が塞がる。代償として、決済後に生成が失敗した場合のリスクは買い手側へ移る。**実地検証済み（2026-08-31）**: クラウド上の実オンチェーン決済 2 回で upfront が期待どおり動作（決済確定 → 生成 → 成果物返却）することを確認。**補足（2026-09-03、④）**: 「決済後の失敗リスクは買い手側へ移る」が実際に起き、買い手が 60 秒で諦めた 2 件を含む成果物なしの支払いが発生した。買い手側の管理は決定31。返金に相当する仕組みの論点は U7 **追記（2026-09-20）**: 決定54 で `upto` を併記するため、この upfront が効くのは `exact` の経路だけになる。`upto` は仕組み上ハンドラ実行後の決済で、「署名は有効だが決済が通らない」危険が `upto` の経路にだけ戻る。引き換えに、失敗時 0 決済（U7 が求めていた返金相当）が `upto` の買い手にだけ与えられる。スキームごとに危険と保護が対になる形で、どちらを推奨に置くかは U10 の結果を見て判断する **追記（2026-09-21）**: `upto` の併記は今回見送ったため（決定54）、upfront は `exact` のみに適用される。スキーム別の危険と保護の対応は将来 `upto` を実装する際の論点として残る |
| 22 | サーバーの実行形態 | express を廃し、`WebStandardStreamableHTTPServerTransport` を素の Lambda ハンドラから使う。zip デプロイ（Docker・ECR 不使用）。MCP セッションはステートレス（`sessionIdGenerator: undefined`）。ローカル開発用に `node:http` の薄いエントリポイントを別に置く | Lambda では実行環境が毎回同じとは限らず、インメモリのセッション Map は温まっているときだけ動く不安定な実装になる（MCP SDK 1.30 のステートレスモードはセッション検証を一切行わない）。express + Lambda Web Adapter でも動くが、素のハンドラなら Docker が一切不要で CDK は NodejsFunction だけで済み、コールドスタートも小さい。なお facilitator への `/supported` 照会は Lambda の INIT フェーズ（10秒制限）を避けるため初回呼び出しへ遅延させる。また GET（単独の SSE ストリーム要求）は SDK へ渡さず 405 で返す。ステートレス + enableJsonResponse では SSE を提供せず、渡すと終わらないストリームが返って Function URL のバッファ応答が永久に未解決になるため（クラウドで Runtime.NodeJsExit として実測。2026-08-31 追記） |
| 23 | 添付ファイルの上限 | 最大1件（ツールのスキーマで強制）。リクエスト全体で base64 後 6MB 以内 | 旧実装は express の 25mb 上限で「3件 × 4.5MB」を受けていたが、Lambda Function URL のリクエスト上限は 6MB（引き上げ不可）で、3件の広告は掲げても受け取れない。上限に収まる1件へ絞り、サイズの目安（元データ約4.4MB）をツールの説明文に明記した（2026-08-30、セルフレビュー指摘の対応でユーザーが決定） |
| 24 | TypeScript からの AgentCore Payments 呼び出し（U1 決着） | `@aws-sdk/client-bedrock-agentcore`（データプレーン）と `@aws-sdk/client-bedrock-agentcore-control`（コントロールプレーン）を採用。SigV4 直呼び・Python 薄層 Lambda は不採用 | 3.1121.0 を一時ディレクトリへ実インストールして型定義を検分し、Payments API の完備を確認（2026-08-31）。データプレーンに ProcessPayment / CreatePaymentSession / CreatePaymentInstrument / GetResourcePaymentToken 等 11 コマンド、コントロールプレーンに PaymentManager / PaymentConnector / PaymentCredentialProvider の CRUD 一式。`PaymentType.CRYPTO_X402` と `CryptoX402PaymentInput/Output` で x402 ペイロードをそのまま搬送できる。決定12 の越境（ap-southeast-1）はクライアントの region 指定で行う |
| 25 | 有料ツール結果の受領方法（U6 決着） | 低レベル API で回避。~~`PaymentClient` で支払いペイロードを作り~~ **支払いペイロードは AgentCore Payments の ProcessPayment で得て（決定24）**、素の `mcpClient.callTool` の `_meta["x402/payment"]` に積む。`x402MCPClient` ラッパは使わない。**買い手は `@x402/*` に実行時依存しない**（ワイヤ型は zod で自前定義、上流の緩さに合わせて extra は任意・未知フィールドは通す） | ラッパは結果の content / isError / _meta のみを写し structuredContent（HTML 本体）を落とす（2026-08-30 実測）。上流は 2.24.0 のまま未修正と再確認（2026-08-31）。低レベル API なら structuredContent が欠けずに届き、パッチ不要。上流への issue 報告は③完了後に改めて判断（保留）。**変更（2026-09-03、PR #2 レビュー）**: 当初は「`PaymentClient` で作り、`@x402/*` 依存は維持」と見込んでいたが、署名を AgentCore Payments に委ねる決定24 によりペイロード生成は ProcessPayment へ移り、買い手側の `@x402/*` 依存は不要になった。ラッパ非使用と `_meta` への積載という核は変わらない |
| 26 | agent-app の使用ブロック | Agent / AuthCognito / KVStore / ApiNamespace / Realtime の5つ | 当初想定の4つに Realtime を追加。決定10 の最終形（ツール結果を Realtime 経由でブラウザへ）に必須のため、④でのブロック追加による手戻りを避けて③時点で確定に含める（配線・結合検証は④）。2026-08-31 ユーザー決定。**補足（2026-09-03）**: ③時点のコードでは Realtime は Agent ブロックが内蔵する分のみで明示的な配線は未着手。逆にスキャフォールド由来の todos デモが DistributedTable を使っており、フロントが依存するため③では残置し④の UI 置き換えで撤去する（確定ブロックには含めない） **補足（2026-09-03、④着工）**: Realtime の購読は `@aws-blocks/bb-agent/client` の `useChat`（React 非依存）で行う。購読の確立→履歴読込→送信の順序はフックが担う。実測結果は④の実装記録に記す |
| 27 | フェーズ③の検証構成 | 売り手ローカルで縦串。billing-mcp は `pnpm dev`（ポート8000）、agent-app も AWS Blocks のローカル開発で動かす。AgentCore Payments のみクラウド実物（ap-southeast-1）を使い、実オンチェーン決済（Base Sepolia）まで通す。Coinbase コネクタは **MANUAL**（CDP の API キー・ウォレットシークレット持参 + AgentCore Identity に保存）で作成。新ウォレットの資金は **CDP faucet**（`scripts/faucet.ts`）で供給 | 無認証エンドポイントの再公開と `cdk deploy` を伴わずに「Agent が支払って有料ツールを使う」縦串を検証できる。ap-southeast-1 に PaymentManager はゼロ件でセットアップはゼロから（2026-08-31 実測）。クラウド結合（売り手再デプロイ）は④で実施。2026-08-31 ユーザー決定。**変更（2026-09-02〜03）**: ①コネクタは当初 QUICK_CREATE（OAuth 同意）だったが、AWS コンソールの支払い画面が白画面のまま進まない事象が3回続き MANUAL へ切替（AWS Marketplace の Coinbase サブスクリプション加入が前提条件なのは両経路共通）。②資金供給は当初「旧買い手からの送金」だったが、旧買い手はガス用 ETH がゼロで ERC-20 送金不可のため CDP faucet へ切替。③ウォレットは作成直後に status ACTIVE でも、**WalletHub（redirectUrl）でエンドユーザーが Delegated signing を許可するまで ProcessPayment は通らない**（許可には有効期限があり、検証では7日で設定）。WalletHub のログインには CDP プロジェクトの Domains 許可リストに `https://hub.cdp.coinbase.com` の追加が必要。**検証完了（2026-09-03）**: Agent が AgentCore Payments で 0.1 テスト USDC を実オンチェーン決済し、HTML（structuredContent）を欠損なく受領・KVStore へ保存。買い手 1.0→0.9 / 売り手 0.22→0.32 USDC、tx 0xa0e35a61…29ca5。**追記（2026-09-06）**: コンソールの「支払い」画面が白画面になる原因はコンソールの言語設定で、English (US) にすると表示される（日本語・English (UK) では描画されない。ユーザーが特定）。コネクタは引き続き MANUAL（`payments-setup.ts`）を正とする。スクリプトで再現でき、CDP の資格情報は一度 AgentCore Identity の credential provider（`coinbaseManual`）に預けたらアプリの Lambda や Amplify の環境変数には要らないため |
| 28 | フェーズ④（結合）の検証構成と完了条件 | agent-app は**ローカル**（`npm run dev`）、LLM はローカルでも Bedrock（`model.local`）、売り手（billing-mcp）は**クラウド**へ再デプロイして結合。完了条件は「ブラウザで依頼すると Agent が実オンチェーン決済で有料ツールを使い、生成されたページが画面に出る」。agent-app のクラウド deploy は⑤へ | ローカルなら selfSignUp と実費 API の組み合わせ（レート制限なし）の穴・`PAYMENT_*` の Lambda 配線・AppSetting 化を④から外せる。canned プロバイダは「依頼文にツール名を含めると発火」であり「依頼するとエージェントが支払う」体験にならないため、ローカルでも Bedrock を使う。③からの繰り越しのうち **buyer API を通る e2e** と **購入単位の冪等キー**（決定30）は④で扱い、**支払い主体の二重化**（利用者ごとの instrument と WalletHub 委任）と **selfSignUp とレート制限**は⑤へ繰り越す。2026-09-03 ユーザー決定（grill-me）。**進捗（2026-09-03）**: ローカル売り手に対してブラウザからの縦串（依頼 → 実決済 → ui:// ホスト描画）と決定31 の防護まで検証済み。売り手のクラウド再デプロイと結合はユーザー判断で同日は見送り、次回セッションで実施する。**追記（2026-09-03、ユーザー決定）**: 次回以降のタスクとして①PaymentSession の作成をアプリに組み込む（`payments-setup.ts` は初回 provisioning と WalletHub 委任の案内に限定し、`PAYMENT_SESSION_ID` の手渡しを無くす。有効なセッションが無ければツールハンドラが `CreatePaymentSession` で切って KVStore に保存し、失効で拒否されたら一度だけ作り直す。上限・期限はアプリ設定に）②ウォレット残高の推移を画面に出す（購入の前後で残高を見せる。取得元は AgentCore Payments の API で取れるか要確認、無ければ Base Sepolia の RPC で `balanceOf`）。なお `maxSpendAmount` は instrument の持ち主単位の枠で、利用者ごとの枠は⑤の「支払い主体の二重化」と合わせて扱う。**追記（2026-09-04）**: ⑤の deploy 先は決定33（Amplify Gen2）。ここに挙げた⑤の残論点は決定33 では扱わず据え置き。**追記（2026-09-05）**: ⑤の残論点は全て解消した。selfSignUp は決定36、支払い主体の二重化は決定39（利用者ごとの instrument ではなく利用者ごとの PaymentSession で解いた）、レート制限は決定40。**追記（2026-09-06）**: 追記②のウォレット残高の表示は決定42 で実装した（取得元は AgentCore Payments の API） |
| 29 | 生成 HTML の描画経路 | ブラウザが MCP Apps の**ホスト**になる。①売り手 `/mcp` へ `resources/read` で `ui://billing-mcp/preview-view.html` を無課金で取得 ②`sandbox="allow-scripts"`（`allow-same-origin` 無し）の iframe に `srcdoc` で入れる ③`@modelcontextprotocol/ext-apps/app-bridge` の `AppBridge` + `PostMessageTransport` で初期化し、`getPurchasedHtml(resultId)` で取った HTML を `sendToolResult({ structuredContent: { html } })` で注入する。**載せる場所（会話の中のカード）と枚数（カードごとに 1 つ）は決定50 で更新した。①〜③の手順そのものは変わらない** | 決定10 の「ブラウザが ui:// を直接取得して iframe 描画」を忠実に実現する形。ツール結果は Agent 側にしか無いため HTML 本体は `getPurchasedHtml` から取り、両経路は「どちらか」ではなく**組み合わせ**で成立する。外部生成 HTML をアプリのオリジンで描画すると XSS になる（PR #2 レビュー指摘25）ため、外側 iframe は不透明オリジン（sandbox + srcdoc）に置く。preview-view 内側の iframe も `sandbox="allow-scripts"` で二重に閉じる。`PostMessageTransport` は `postMessage(msg, "*")` で送るため不透明オリジンでも通る（1.7.5 の実装で確認）。別オリジン配信は⑤の論点。不採用: getPurchasedHtml の HTML を直接 iframe に出す最小案（MCP Apps の UI が売り手の展示物で終わる）。2026-09-03 ユーザー決定（grill-me） |
| 30 | 購入単位の冪等キー | 購入 1 件ごとに `resultId`（UUID）を先に採番し、KVStore の保存キーと ProcessPayment の `clientToken` の両方に同じ値を使う | ③は `clientToken` を署名のたびに `randomUUID()` で採番しており、購入物（resultId）と Payments 側の冪等キーが結びつかなかった（PR #2 レビュー指摘）。同じ値にすれば、SDK の自動再試行は同一トークンで冪等になり、Payments 側の記録から購入物へ辿れる。限界: Agent ブロックのツールハンドラは `toolUseId` を受け取らないため、LLM が同じ依頼でツールを再度呼んだ場合は別の購入として扱う（それは再試行ではなく新規の購入とみなす）。2026-09-03 |
| 31 | 決済後の失敗と二重支払いの防護（買い手側） | ①有料ツール呼び出しのタイムアウトを売り手の上限に合わせる（`BUYER_TOOL_TIMEOUT_MS`、既定 600 秒。MCP SDK の既定 60 秒は使わない）②支払い証明を送った後の例外は `PaidToolError`（支払い済み・EIP-3009 nonce 付き）として区別し、レシートを KVStore に残す ③同じ会話に「支払い済み・成果物なし」の購入があれば、次の購入は `interrupt` で人の承認を要求し、LLM の自動再試行では支払わせない ④システムプロンプトでも自動再試行を禁じる（補助。保証は③が担う） | 2026-09-03 のブラウザ検証で、売り手の生成が 60 秒を超えたため買い手の MCP クライアントが決済後に諦め、LLM が「失敗」と見て自動再試行し、1 回の依頼で 2 回支払った（並行して行われた別セッションの操作分と合わせ、成果物なしの支払いが 4 件 = 0.4 テスト USDC）。③では生成が短く露見しなかった。決定21 の upfront は「決済後の失敗リスクを買い手が負う」選択であり、そのリスクを買い手が管理する仕組みが無かった。売り手側の冪等化（同じ支払い証明の再提示には再決済せず成果物を返す。Lambda に保存先が要る）は⑤以降の課題として残す。毎回の承認を必須にする案は「エージェントが自律的に支払う」筋書き（決定10）を弱めるため不採用。2026-09-03 ユーザー決定。**注記**: ⑤でデプロイする際は Agent を実行する AsyncJob の Lambda タイムアウトが `BUYER_TOOL_TIMEOUT_MS` を包含している必要がある（短いと①が働かない）。**補足（2026-09-03、PR #3 レビュー）**: ③の「未解決の支払いがあれば承認を要求」は当初、会話全体を対象に判定していたため、一度事故が起きた会話では承認して買い直しが成功した後もすべての購入が承認待ちになり、「毎回の承認を必須にする案は不採用」とした本決定の意図と食い違っていた。判定対象を**最後に成功した購入より後の失敗**に限定して是正した（`unresolvedPayments`）。**追記（2026-09-06、決定48）**: ③の判定は会話履歴だけを見ており、ウォレットと枠が利用者ごとで会話をまたぐのに防護だけが会話に閉じていた。事故の残る会話を離れて新しい会話を作れば承認を経ずに買い直せる（画面の「新規会話」で防護が外れる）状態だったため、利用者ごとの記録との**和**で判定するよう決定48 で広げた。また②の `PaidToolError` が覆うのは「売り手へ支払い証明を送った後の例外」だけで、`ProcessPayment` 自体の失敗（タイムアウト等、支払いの成否が不明なもの）は対象外だった。これも決定48 で `UncertainPaymentError` として区別し、同じ防護に乗せた |
| 32 | AWS 構成図の形式と置き場所 | `docs/architecture.drawio.png`（draw.io の XML を埋め込んだ PNG）に 1 枚で持つ。中身は AWS 公式アイコン（mxgraph.aws4）で、AWS Cloud → リージョン → 買い手／売り手の入れ子にし、有料経路（赤）と無課金経路（青）を線の色だけで分ける（どちらも実線）。番号つきの矢印と、図の下の「処理の流れ」表で 1 回の購入の順序を示す | 画像だけだと修正のたびに描き直しになるため、閲覧（GitHub 上でそのまま見える）と編集（draw.io で開けば元の図が戻る）を 1 ファイルで両立させた。`.drawio` の中間ファイルは PNG に同じ XML が入っているため残さない。記載内容は `agent-app` の `cdk synth` 結果と `billing-mcp/stacks` の CDK 定義から起こしており、想像で描いた箱は置かない。本図はフェーズ⑤の deploy 時に作られる構成であり、④時点の実際の姿（買い手はローカル・売り手はスタック削除済み）とはずれる。この差は図の中に「現況」として明記して埋めた。2026-09-03。**変更（同日、ユーザーレビュー）**: ①初版は AgentCore Payments を売り手の右（左右並び）に置いており「買い手のものなのに売り手を挟んで遠い」と指摘を受けた。ap-southeast-1 の枠を買い手の真下へ移し、④ の矢印が Handler から真下へ落ちる形にした。②線の交差が多かったため、Handler のラベルを上側へ移して下辺を空け、右辺から出る矢印の出口の高さと通路の x 座標を固定して交差をゼロにした。③「線が黒い」はアイコン内部の絵柄のことだった。draw.io の `resourceIcon` は絵柄を `strokeColor` で塗り（既定 `#000000`）、初版は `strokeColor=none` としていたため黒く出ていた。AWS 公式どおり `strokeColor=#ffffff` に修正（15 個）。なお最初は枠線の色の話と取り違え、枠線・文字色を `light-dark()` にする変更を先に入れた。こちらは暗い背景で開いたときの見やすさとして残している。④図中の「決定N」参照（23 箇所）はユーザー指摘で全て外した。図だけを見る人には意味の取れない番号であり、設計録への導線はタイトル欄の docs/DESIGN.md への言及だけで足りる。⑤ の矢印が上へ迂回していたのは、DynamoDB を下段へ移す前の経路が残っていただけだったので L 字 1 本に直した。⑥無課金経路は当初「青の破線」だったが、破線は意味が伝わりにくいとの指摘で実線にし、色だけで区別することにした。⑦買い手側は、`aws-blocks/index.ts` と `buyer-agent.ts` で宣言している Block のインスタンス単位（`AuthCognito「auth」` / `Agent「buyer-agent」` / `KVStore「purchased-html」` / `ApiNamespace「buyer」「api」`）で囲む形に改めた（ユーザー指摘）。Realtime は宣言が無く Agent の内側の要素なので Agent の囲みに入れ、CloudFront と S3 は Block ではなく CDK の `Hosting` construct なので「Block ではない」と注記した別の囲みにし、全 Block が共有する `Handler` Lambda はどの囲みにも入れず中央に置いた。DynamoDB は所属 Block ごとに 4 つに分けた。**追記（2026-09-04、決定33）**: ⑤の deploy 先を Amplify Gen2 + Amplify Hosting に変えた。図の「Block ではない」囲み（CloudFront + S3 の `Hosting` construct）は CDK 直 deploy のときの姿で、Amplify 経路ではフロントの配信が Amplify Hosting に、API 呼び出しは CloudFront を経由せず API Gateway へ直接（別オリジン）になる。Block の id も `Scope 'app'` / `Agent 'buyer'`（+ `BlocksBackend 'b'`）に変わった（決定33 の追記）ため、図の `AuthCognito「auth」` 以外のラベル（`Agent「buyer-agent」` 等）は旧 id である。図の差し替えは Amplify での本番 deploy が通ってから行い、そのときラベルも直す。**差し替え（同日夜）**: main の Amplify deploy が成功したので差し替えた。「Block ではない」囲みは Amplify Hosting（配信）と Amplify ビルド（`amplify.yml` の `ampx pipeline-deploy`）に、① はブラウザから API Gateway への直接呼び出し（別オリジン）に改め、Block id を `app` / `buyer` / `b` に直した。決定35 の KVStore `payment-session` と決定36 の自己サインアップ無効も反映。出典は `amplify/backend.ts` の合成結果 **追記（2026-09-05、見た目の整形）**: ユーザーレビューで「縦横が揃っていない・線が無駄に曲がる・囲みから外れる」と指摘を受け、座標とラベル位置を整えた（実装記録 2026-09-05）。配置の原則は変えていないが、ap-southeast-1 の枠だけは買い手の枠の右端に揃えるのをやめ、Payments のラベルが収まる幅まで広げた。Handler のラベルは 4 行にし、API Gateway からの線が文字を貫かないようにした **追記（2026-09-06、Lambda が何者かの注記）**: ユーザー指摘「買い手のエージェントが動いているのは Lambda のはずなのに、Agent「buyer」の囲みに Lambda が入っておらず分かりにくい」。囲みを Block の宣言単位で切り、全 Block 共有の Handler をどの囲みにも入れない、という上記の判断自体は合成結果（Lambda は 1 本で、API Gateway REST の統合・WebSocket の 3 ルート・SQS のイベントソースを兼ねる。2026-09-03 の CDK 直 synth での観測で、決定33 の Amplify 移行後に再合成はしていない）に即しているため変えない。アイコンを Agent の囲みへ移すと実態に反し、②・⑨ の配線も壊れる。置き方は保ったまま、読み手への説明を 2 行足した。①凡例に「囲みは宣言の単位で、中の箱は Block が作る AWS 資源。買い手のコードが動くのは Handler Lambda 1 か所だけ」②Agent「buyer」の囲みの下辺に「実行は Handler Lambda。会話ループ・LLM 呼び出し・MCP ツール呼び出し・x402 の署名はこの関数の中で動く」。Handler のアイコンのラベルは触っていない（4 行に整えて縦線が文字を貫かないようにした経緯があり、字数を増やすと再発する）。凡例は図全体に掛かる箱にあり、同じ図には売り手の McpFunction も描かれているため、①の主語は「買い手の」に絞った **追記（2026-09-07、BlocksBackend の囲みを足す）**: ユーザー指摘「Lambda が Blocks 製なら図が違うのでは」。調べたところ誤っていたのは Lambda ではなく **API Gateway（REST）の所属** だった。REST API と Handler Lambda はどちらも `BlocksBackend`（`@aws-blocks/core` の `setupBlocksInfra`）が作るもので、`ApiNamespace` は CDK 構造物を一つも作らない（実装はハンドラにマークを付けて返すだけ）。「中の箱は Block が作る AWS 資源」という凡例に対して、API Gateway を `ApiNamespace「buyer」「api」` の囲みに入れていたのは、図が自分の凡例と矛盾していたことになる。よって 2026-09-06 の「全 Block 共有の Handler はどの囲みにも入れない」は撤回し、API Gateway と Handler をまとめて破線の囲み `BlocksBackend「b」（全 Block 共有）` に入れた（代案として示した 2 案からユーザーが選択）。破線は「Blocks が作る共有インフラ」の印として凡例に見本を足し、`ApiNamespace` が資源を持たない宣言であることは凡例の注記に書いた。Handler のラベルは決定34 の改訂に合わせて 「PAYMENT_* は合成時の環境変数から（`aws-blocks/runtime.cdk.ts`）」に更新。囲みは左へ 17 広げ（`x=780 w=228`）ラベルが破線を跨がないようにし、タイトルは 10pt にして ① の矢印と重ならないようにした。アイコンの座標は動かしていないので、Handler と API Gateway に付く 12 本の結線はそのまま。出典の日付も 2026-09-07 に更新。なお破線を使うのは囲みだけで、経路（線）は⑥ の判断どおり実線のまま——線は色だけで有料／無課金を分ける規則を変えていない。「現況」の箱は 2026-09-05 のまま据え置いた（deploy の状態を今日確かめたわけではないので、日付だけ進めない） **是正（2026-09-07、PR #23 のセルフレビュー）**: ①出典の日付を 2026-09-07 に進めたが、この日に走らせたのは CDK 直の `cdk synth` だけで `ampx` の再合成はしていない。出典は「Amplify 合成結果」を名乗っているので「合成結果は 2026-09-05 時点。2026-09-07 に所属の誤りを修正」に改めた（本項 2026-09-06 の「Amplify 移行後に再合成はしていない」と揃う）②買い手の囲みの「ネストスタック「b」」は誤り。ネストスタックは `backend.createStack('blocks')` の `blocks` で、`b` は `BlocksBackend`（`NestedStack` ではなく `Construct`）の id。内側に `BlocksBackend「b」` を足したことで「b」が二重になったため `blocks` に直した③Handler のラベルを 1 行太らせてしまい、赤い縦線との余白が実測で 14px から 3px に縮んでいた（本項 2026-09-06 の「字数を増やすと再発する」という申し送りに反していた）。3 行目を 2 行に割り、ラベルの左端を base と同じ x=799 に戻した |
| 33 | agent-app のクラウド deploy（フェーズ⑤の土台） | Amplify Gen2 + Amplify Hosting を正とする。`amplify/backend.ts` の `defineBackend({})` に `backend.createStack('blocks')` でネストスタックを切り、そこへ `BlocksBackend.create()`（`@aws-blocks/blocks/cdk`。公式 devguide「Integrating with existing infrastructure」Pattern 1）で既存の `aws-blocks/` を丸ごと載せる。Amplify 側の `defineAuth` / `defineData` は使わず、認証は従来どおり `AuthCognito` Block。フロント（Vite）は Amplify Hosting が配信し、ブラウザは `/.blocks-sandbox/config.json`（ビルド時に `amplify_outputs.json` の `custom.blocks_api_url` から生成）で API Gateway の絶対 URL を知り、別オリジンで呼ぶ。そのため Lambda に `CORS_ALLOWED_ORIGINS`（amplifyapp.com のオリジン）を渡し、`AuthCognito` は `crossDomain: true`（Cookie を `SameSite=None; Secure; Partitioned` に）。`amplify/` の雛形は `npm create amplify` の生成物を写して手で置く。CDK 直の `npm run deploy`（`BlocksStack` + `Hosting`）は退路・比較用に残す | ユーザー要件（Amplify コンソールでバックエンドとフロントを一元管理し、git push で deploy する）。2026-09-04 ユーザー決定（grill-me）。根拠: `BlocksBackend` は公式 API で、`@aws-blocks/core` 0.3.x の `blocks-backend.ts`（`fullId`）に「Amplify Gen2 `backend.createStack('blocks')` のネストスタック」が想定ケースとして明記されている。公式 CLI `create-blocks-app` 0.1.21 は `amplify/backend.ts` を検出すると `templates/amplify/`（`amplify/blocks.ts`・`createBlocksBackend`・`NODE_OPTIONS="--conditions=cdk"` 付きの `amplify.yml`・`cross-env` のスクリプト）を生成するので、本決定はその生成物を手本にした（CLI 自体は `aws-blocks/` を雛形で上書きするため既存プロジェクトには使えない。決定13「手書きしない」の例外だが、公式生成物の写しに限る）。AWS Blocks の公式 devguide は Amplify を「補完関係（Amplify = hosting / CI/CD / マネージド体験、Blocks = infrastructure-from-code）」と位置づけており、本構成はその線に沿う。`amplify/` を `npm create amplify` で作らないのは、同コマンドが `aws-cdk-lib@2.244.0` を固定で入れて Blocks 側の 2.267 と衝突するため（Amplify スキルも「カスタムパイプラインでは手動インストール」を認めている）。フロントと API を別オリジンにするのは、Amplify Hosting の rewrite（200 プロキシ）がアプリ単位の設定でブランチごとの API URL に追随できず、Cookie 転送の挙動も未確認なため。別オリジン構成は `AuthCognito` README（`crossDomain`）と core README（`CORS_ALLOWED_ORIGINS`「フロントを別ドメインで配信する場合」）に公式の手順があり、現行の sandbox モード（localhost のフロント + API Gateway）と同じ形。前提: AWS Blocks は 2026-06-16 公開のパブリックプレビューで、統合の作法は変わりうる。壊れたときの退路が CDK 直 deploy。⑤の残論点（`PAYMENT_*` の配線と AppSetting 化・selfSignUp とレート制限・支払い主体の二重化）は本決定の範囲外で決定28 のまま。**追記（同日、Block id の短縮）**: ローカルで `amplify/backend.ts` を `--conditions=cdk` で合成したところ、Agent ブロック内蔵の S3 バケット名 `amplify-agentapp-<sandbox名>-sandbox-<hash>-blocks-agent-app-buyer-agent-sn` が 78 文字で S3 の 63 文字上限を超えて失敗した。Blocks は S3 名を「スコープ id の連結」で決め、短縮もハッシュ化も意図的にしない（`bb-file-bucket/bucket-name.js`: 名前が deploy ごとに変わるとデータを迷子にするため、開発者が id を短くせよ）。Agent は内部で `FileBucket 'sn'` を必ず作り、既存バケットの指定もできない。Amplify のルートスタック名は sandbox で `amplify-agentapp-<識別子>-sandbox-<hash10>`（36 + 識別子）、ブランチで `amplify-<appId14>-<ブランチ>-branch-<hash10>`（41 + ブランチ名）と長く、id を縮める以外に手が無い。`BlocksBackend 'blocks'→'b'`、`Scope 'agent-app'→'app'`、`Agent 'buyer-agent'→'buyer'` とし（接尾辞 `-b-app-buyer-sn` = 15 文字）、ブランチ名 7 文字以内・sandbox 識別子 12 文字以内（既定の OS ユーザー名がちょうど 12）を制約として README に明記した。最短案（`b`/`a`/`ag`、ブランチ 12 文字まで）は可読性を落とすため不採用。まだ何も deploy していないため移行は不要だが、CDK 直経路の物理名も同時に変わる。ユーザー決定。**追記（2026-09-07、`amplify/` の責務）**: `amplify/` に残すのは Amplify 固有のものだけとし（`backend.ts` の入口・`cors-origins.ts`・クロスドメイン Cookie）、共有 Lambda への実行時設定と IAM は `aws-blocks/runtime.cdk.ts` に集約した（決定34 の改訂を参照）。`aws-blocks/amplify.cdk.ts` が既に「Amplify 向けの CDK 糊を `aws-blocks/` に置く」公式テンプレートの前例になっている。なお共有 Lambda 自体は Blocks 製（`BlocksBackend` の `setupBlocksInfra` が API Gateway と併せて作る）で、Amplify が作っているわけではない。Blocks に「関数単体」の Building Block は無く（`bb-lambda-compute` は「Internal / not yet customer-facing」で公開 API に出ていない）、アプリのコードは 1 本の共有 Lambda で動く。ユーザー決定 |
| 34 | 実行時設定と IAM の Lambda への配線（⑤） | 実行時設定（`PAYMENT_MANAGER_ARN` / `PAYMENT_INSTRUMENT_ID` / `BILLING_MCP_URL` / `PAYMENTS_USER_ID` / `PAYMENT_MAX_AMOUNT` / `PAYMENT_PAY_TO` / `BUYER_TOOL_TIMEOUT_MS` / `PAYMENT_SESSION_MINUTES` / `PAYMENT_SESSION_MAX_USD`）は、合成時のプロセス環境変数（Amplify Hosting ではコンソールのビルド環境変数、sandbox ではシェル）から許可リストで拾い、`amplify/blocks.ts` が `blocks.handler.addEnvironment` で共有 Lambda に写す（`amplify/runtime-env.ts`）。ブランチ deploy では `PAYMENT_MANAGER_ARN` / `PAYMENT_INSTRUMENT_ID` / `BILLING_MCP_URL` を必須とし、無ければ合成で落とす。sandbox では欠けても通す（認証と API の疎通だけを見る用途があるため）。`BUYER_TOOL_TIMEOUT_MS` は共有 Lambda のタイムアウト（900 秒）以下でなければ合成で落とす。IAM は同じ場所で `blocks.handler.addToRolePolicy` により `bedrock-agentcore:CreatePaymentSession` / `GetPaymentSession` / `ProcessPayment` を付ける。AppSetting（SSM）化は採らない。**配線の置き場所（`aws-blocks/runtime.cdk.ts` の `wireRuntime`）と、必須値を落とす条件（deploy のときだけ）は2026-09-07 の改訂で更新した。許可リストと書式の検証そのものは変わらない** | 候補は①ビルド環境変数の透過②AppSetting 化（決定28 の本筋）。値はいずれも識別子で秘密ではなく、Amplify コンソールの環境変数はビルドにしか届かないため、②でも初期値の入口は同じ環境変数になる。②は `buyer-agent.ts` と `payments/` の読み取りを `process.env` から非同期の `get()` に書き換える必要があり、ローカル経路にも波及する。①なら実行時コードは無変更で、変更は再ビルドで反映される（実行時に変えたい値が現れたらその項目だけ AppSetting に出す）。Agent のジョブは AsyncJob が共有 Lambda に SQS イベントで流す形（`bb-async-job` の `index.cdk.js`）なので、共有 Lambda の環境変数と IAM がそのまま決済処理に届く。共有 Lambda のタイムアウトは `@aws-blocks/core` `blocks-backend.js` で 900 秒固定であり、`BUYER_TOOL_TIMEOUT_MS` 既定 600 秒（決定31）を包含する。IAM のリソースは当初 `*`（AgentCore Payments の各アクションが受け付けるリソース形式を確認していないため）。**改訂（2026-09-05、PR #6 レビュー）**: AWS のリファレンスポリシーに合わせ `arn:aws:bedrock-agentcore:*:<account>:payment-manager/*` に絞った（リージョンは決定12 のクロスリージョン呼び出しのため `*` のまま）。ロールの分離については決定37 と U8 を参照。2026-09-04 ユーザー決定（grill-me）。**実測（2026-09-05、main）**: 環境変数は Amplify の「アプリ単位」に置いた（ブランチ単位でなくてもビルドに届く。ブランチが 1 本のため一括で見られる方を採る）。`PAYMENT_MANAGER_ARN` に `payments-setup.ts` の表形式の出力 3 行が丸ごと貼り付いた状態で合成が通り、Lambda にもそのまま写っていた（許可リストは値の有無しか見ない）。CLI（`update-app`）で是正し再ビルドした。ARN や URL の書式検証は足さない（人が貼る値の検証は過剰というユーザー判断。代わりに README で注意を促す）。IAM を `payment-manager/*` に絞った状態（決定37）で `CreatePaymentSession` / `ProcessPayment` が通ることを main の実決済で確認。**改訂（2026-09-07、配線の置き場所）**: 配線を `amplify/blocks.ts` から `aws-blocks/runtime.cdk.ts` の `wireRuntime` へ移し、Amplify 経路と CDK 直経路（`aws-blocks/index.cdk.ts`）の両方から呼ぶようにした（`amplify/runtime-env.ts` → `aws-blocks/runtime-env.ts`）。理由: ①この配線は Amplify 固有ではなく、共有 Lambda に対する設定なので CDK 直 deploy にも同じものが要る ②実際 CDK 直経路には配線が無く、`npm run deploy` / `npm run sandbox` は決済のできない Lambda を作っていた（決定33 の退路が退路になっていなかった）。Blocks 側に寄せられる下限は `*.cdk.ts` の層まで——`Scope` は条件付き exports で二重に解決され、実行時側（`@aws-blocks/core` の `common/index.js`）の `Scope` は `handler` も `executionRole` も持たないため、`aws-blocks/index.ts` には書けない。`handler.addEnvironment` 自体は core の README が CORS の例で示す公式の手順。副作用として、CDK 直の非 sandbox 合成は必須値が無いと落ちるようになった（これまでは黙って通っていた）。合成後のテンプレートで環境変数とポリシーを見る Template テスト（`aws-blocks/runtime.cdk.test.ts`）を追加。ユーザー決定 **改訂（2026-09-07、必須値を落とす条件）**: 当初は「sandbox 以外なら落とす」としたが、`@aws-blocks/core` の `destroy()` は `cdk destroy` を `sandboxMode` 無し・`.env.production` も読まずに起動するため、CDK 直経路では `npm run destroy` / `cdk diff` まで落ち、決定33 の退路が「畳めない退路」になっていた（PR #5 で `ampx sandbox delete` が CORS のガードに掛かったのと同型）。`runtimeEnvironment` の引数を `sandboxMode` から `requireAll` に替え、Amplify 経路は `!sandboxMode`、CDK 直経路は `!sandboxMode && BUYER_CDK_DEPLOY === "true"`（`scripts/deploy.ts` が立てる）で決める。あわせて `wireRuntime` が載せたキー名を合成時に 1 行出すようにした（取得元が対話シェルにもなったため、検証用に export したままの値が黙って焼き込まれるのを気づけるように。値は出さない）。PR #23 のセルフレビューでの是正 |
| 35 | PaymentSession のアプリ内作成（決定28 追記①） | `payments-setup.ts` はセッションを切らず、PaymentManager / Connector / Instrument の provisioning と WalletHub 委任の案内に限定する（`PAYMENT_SESSION_ID` の手渡しを廃止）。ツールハンドラは購入のたびに `payments/payment-session.ts` の `paymentSessionSource` で有効なセッション ID を得る。KVStore `payment-session`（TTL 有効）にウォレットの持ち主 ID（`PAYMENTS_USER_ID`）をキーとして `{ paymentSessionId, expiresAt }` を保存し、期限（60 秒の余裕を引く）内なら使い回し、無ければ `CreatePaymentSession`（既定 `PAYMENT_SESSION_MINUTES=60`、`PAYMENT_SESSION_MAX_USD=1.00`。決定34 の環境変数で変更可）で切って保存する。`ProcessPayment` がセッション起因（`ResourceNotFoundException`、または例外メッセージに session を含む `ValidationException` / `ConflictException`）で拒否されたら、一度だけ作り直して再試行する。**改訂（2026-09-05、PR #6 レビュー）**: ①支出上限の超過は作り直しの対象から外した（決定37）②`renew` には拒否されたセッション ID を渡し、別の購入が既に作り直していればその有効なセッションに乗る③読めた記録があるときは書き込みを条件付き（`ifValueEquals`）にし、読んでから書くまでに別の購入が書いていたら相手を残す（作ったセッションはその購入にだけ使う）。記録を読めなかったときは条件を付けない——`get` は期限切れを `null` にするが実体は消さず（TTL 掃除は最大 48 時間）、`ifNotExists` は実体の有無を見るため、付けると期限切れの実体が残る間ずっと保存できなくなる。`ProcessPayment` の失敗は署名前なので再試行しても支払いは二重にならない | セッションは 60 分で失効するため、環境変数で Lambda に渡す方式（決定34）では deploy から 1 時間で決済が止まる。クラウドで決済を持続させるには本項が前提であり、⑤の「その後」ではなく B の一部として繰り上げた。セッションは instrument の持ち主単位でアプリ全体に 1 つ（`maxSpendAmount` は持ち主単位の枠。決定28）。作り直しで枠が時間ごとに戻るため、上限は「1 セッションあたり」であって累積の上限ではない。累積の上限はウォレット残高が担い、利用者ごとの枠は「支払い主体の二重化」（決定28）で扱う。拒否の判定に使う例外の種類は SDK の型から推定したもので、sandbox の実測で確定させる。2026-09-04 ユーザー決定（grill-me）。**実測（同日、sandbox）**: 作成 → 使い回し → 削除後の作り直しの 3 経路を実オンチェーン決済で確認。削除済みセッションでの `ProcessPayment` は `ValidationException`（message `Payment session not found: <id>`）で拒否され、`isSessionRejection` が拾って一度の作り直しで成功した。注意点: セッションの残枠は署名時点で差し引かれ、売り手側の決済確定（settle）が失敗して資金が動かなかった購入の分も戻らない（1.00 → 0.8 USD）。枠は「署名した回数」の上限として読むこと。**実測（2026-09-05、main）**: 本番の DynamoDB で作成 → 使い回し（2 回）を実オンチェーン決済で確認した（セッション残枠 1.00 → 0.7 USD）。作り直しの経路は、削除スクリプトの実行ミスに気づかないまま 3 回目を発注したため main では未検証（sandbox の 2026-09-04 実測のみ）。**改訂（2026-09-05、決定39）**: 記録のキーをウォレットの持ち主から利用者（Cognito の sub）に変え、上限と期限は利用者 1 人あたりの枠になった。本項の「セッションは持ち主単位でアプリ全体に 1 つ」「利用者ごとの枠は決定28 で扱う」は改訂前の記述として残す。なお main に残置した持ち主キー（`sample-user-1`）の記録は、決定39 の適用後は読まれない。作り直しの実測は改めて `DeletePaymentSession` を打って行う |
| 36 | selfSignUp を閉じる（決定28 の⑤の残論点） | `AuthCognito` の `selfSignUp` は `BUYER_SELF_SIGNUP=true` のときだけ有効にし、既定は無効。ローカルの `npm run dev` と `npm run test:e2e` は npm スクリプトがこの変数を付ける。クラウド（sandbox・ブランチ）では利用者を管理者が作る（Cognito コンソール、または `auth.admin.createUser`）。レート制限は本決定では扱わない（**追記（2026-09-05）**: 決定40 で扱った） | 決定35 でセッションが作り直されると上限は時間ごとに戻り、実質の上限はウォレット残高になる。その状態で selfSignUp を開いたまま公開 URL（Amplify Hosting）に出すと、誰でもサインアップしてウォレットから支払わせられる。main に `PAYMENT_*` を設定して実決済を通す前に塞ぐ（sandbox での実決済の検証は本決定の前でも構わない）。合成時に `BLOCKS_STACK_NAME` はまだ無く「クラウドかどうか」で切り替えられないため、明示の環境変数で開ける形にした。2026-09-04 ユーザー決定（grill-me） |
| 37 | セッションの支出上限を上限として機能させる（決定35 の改訂。PR #6 レビュー） | `ProcessPayment` の拒否を「作り直せば解消するもの（失効・削除済み）」と「支出上限・残高の不足」に分け、後者（`isSpendLimitRejection`。`ValidationException` / `ConflictException` のうち message が `limit` / `exceed` / `budget` / `insufficient` を含むもの。例外の種類で絞るのは `ThrottlingException` の `Rate exceeded` などを「上限に達した」と誤って伝えないため）では作り直さず失敗させる。`PAYMENT_SESSION_MAX_USD` / `PAYMENT_SESSION_MINUTES` の書式は `amplify/runtime-env.ts` が合成の時点で検証して落とし、実行時（ローカル）に既定へ戻す場合は `console.warn` を出す。IAM は `payment-manager/*` に絞るが、`CreatePaymentSession` と `ProcessPayment` のロール分離は行わない | AWS の AgentCore Payments IAM ガイドは「PaymentSession の書き込み権限と `ProcessPayment` を同一ロールに置くな（新しいセッションを切って上限を迂回できるため）」と明示している。決定35 の実装はこの迂回を IAM で許すだけでなく**アプリが自動で実行**していた（上限超過で拒否された支払いが、その場で新しい枠を切って成立する）。決定35 の理由欄が開示していたのは「枠は時間ごとに戻る」ことまでで、「上限が一度も支払いを止めない」ことは意図していなかった。決定31 が「金が想定外に動いたら人を挟む」としている方針とも逆行するため、上限超過は失敗させる側に倒す。ロール分離については、本アプリは共有 Lambda 1 つがセッション作成と支払いの両方を実行するため、ロールを分けても同じ identity が双方を握ることに変わりはなく、分離では要件を満たせない（U8 に残す）。上限の担保はアプリ側の判定が負う。2026-09-05（PR #6 のレビュー指摘を受けて）。**追記（2026-09-07）**: 本項が指す `amplify/runtime-env.ts` は `aws-blocks/runtime-env.ts` へ移した（決定34 の改訂） |
| 38 | ドキュメント整備の方針（README のわかりにくさへの対応） | README.md（ルート）と agent-app/README.md を改訂した。ルート README には「ドキュメントの読み方」を新設した。README・AGENTS.md・DESIGN.md・implementation-log.md の役割を案内し、各ファイルへリンクを張った。あわせて x402・MCP Apps・AgentCore Payments の一言説明も加えた。ステータス節も実態に合わせて書き直した。「⑤の残論点は決定28」という古い書き方をやめ、決定34〜36 で大半が解消済みであることと、レート制限・支払い主体の二重化が未着手であることを明記した。agent-app/README.md は内容量を変えずに `###` 見出しで再構成した。構成は「アーキテクチャ」と「運用上の注意」に、コマンドは「ローカル開発」と「Amplify sandbox」に、クラウド deploy は「スタック構成」「ネットワークと命名」「実行時設定と検証」に、それぞれ分けた。加えて LICENSE（MIT。著作権者 `k-ibaraki`）を新設した。決定録への案内はルート README の「ドキュメントの読み方」だけに置く。各アプリの README からは決定N の参照を全て外し（agent-app 14件・billing-mcp 6件）、そのファイルの記述だけで内容が分かる形にする。番号が「なぜ」を担っていた箇所は、理由を本文に書き下ろす。3つの README のステータス記述は、記録と突き合わせて実態に合わせた。DESIGN.md の1枚表形式は今回は変更しない | ユーザー指摘「README が分かりにくい」を grill-me で分解した。実測ではルート README（43行、決定番号への参照3件）はむしろ簡潔だった。真に長大だったのは agent-app/README.md（97行・9.4KB、参照14件）である。ユーザーとの確認の結果、対象は両 README の「粒度・順序」と「第三者向け説明の不足」、そして「どこから読めばいいか迷う」という導線の問題だと判明した。本サンプルは今後公開する前提である（2026-09-05 ユーザー回答）。決定録という経緯を残す仕組み自体は公開時の強みとして維持しつつ、入口となる README を第三者が迷わず読める形に直した。**訂正1（同日）**: 初版は「billing-mcp/README.md と DESIGN.md の1枚表形式は対象外」とファイル単位で線引きしていた。このうち DESIGN.md はユーザーへの質問で選択肢に出して選ばれなかったものであり、根拠がある。しかし billing-mcp/README.md の除外は一度も論点に上げておらず、合意を経ていない。担当（Claude）が「言及がない＝対象外」と推論しただけのものを、ユーザーの回答であるかのように提案文へ混ぜて記載していた。ユーザーの指摘によりこの経緯を記録に残す。ドキュメント修正はコストが低く、ファイル単位でスコープを区切る理由も無いため、以後この線引きは行わない。**訂正2（同日、セルフレビュー）**: 上記の除外は実害を生んでいた。売り手スタックを「検証後に削除済み」とする記述が billing-mcp/README.md とルート README の計3箇所に残っていたが、実際には 2026-09-04 の再 deploy 以降そのまま置いてある。billing-mcp/README.md を対象から外していたため、この食い違いを見落としていた。あわせて次の2点も直した。①agent-app/README.md の冒頭が「売り手のクラウド結合は未実施」のままだったが、sandbox ではクラウドの買い手と売り手を結合して実オンチェーン決済まで通っている。②ルート README が sandbox の到達点を「疎通を確認」と過小に書いていた。**訂正3（同日、ユーザー指摘）**: 決定N の扱いを二度も取り違えた。ユーザーの指示は当初から「各アプリの README は外部を参照しないと分からない情報を消し、そのファイル内だけで理解できるようにする」であり、決定32 でも同じ理由（番号だけでは意味が取れない）から構成図の決定N参照23箇所を全て外した前例がある。にもかかわらず、担当（Claude）は一度目に「導線の1行だけを消す」と狭く解釈し、二度目には逆に「DESIGN.md 決定N」へ揃えて外部参照を強めた。指示ではなく自分の立てた枠組み（記法の不統一という問題設定）に沿って解釈したことが原因。最終的に両 README から決定N参照を全て外し、番号が理由を担っていた箇所（ウォレットのリージョン、採らなかった構成）は理由そのものを本文に書いた。DESIGN.md の1枚表形式は、ユーザーが実際の困りごととして選ばなかった論点であり据え置く。**訂正4（同日、整合性の再確認）**: 古い記述を直す作業の中で、新しい誤りを書き込んでいた。ルート README と agent-app/README.md に「main ブランチへの deploy は未実施」と書いたが、決定32 の差し替え記録と構成図の現況注記のとおり、main の Amplify deploy は 2026-09-04 夜に成功している。未実施なのは main のブランチ環境変数への `PAYMENT_*` の設定と main での実決済である。**訂正5（同日、ユーザー指摘）**: そもそも README に書くべきでない内容だった。「2026-09-04 に deploy 済み」「スタックを置いてある」は著者の AWS アカウントの在庫状況で、読者には見えず・使えず・検証できず、スタックを消せばまた古くなる。これは作業記録（implementation-log.md）の領分である。「フェーズ④〜⑤」も読者が知らない内部語彙で、決定N を外したのと同じ問題を残していた。README のステータスは在庫ではなく「このサンプルで何がどこまで動くか」を書く。日付・スタック名・フェーズ番号・内部タスクの消化状況は README に置かない。訂正1〜4 は、置くべきでない文の正確性を直し続けていたことになる。**訂正6（同日、ユーザー指摘）**: ここまでの一連で、当初の依頼「README が分かりにくい」にはほとんど答えていなかった。正確性・一貫性・分類という機械的に検証できることばかりを直し、読者の疑問に順番に答える作業をしていない。とくにリード文は、用語説明を括弧で押し込んだ結果、1文・5行・入れ子の括弧3つになり元より読みにくくなっていた。3つの README を読者の疑問の順に組み直す。①1〜2文で「何ができるか」を具体的に書く ②用語説明はリードから独立した節へ出す ③「動かす」を作って上位に置く（従来どこにも無かった）④メタな案内（ドキュメントの読み方）は末尾へ下げる ⑤解読しづらい ASCII 略図をやめ、構成図 PNG を本文に埋め込む。各アプリの README も冒頭に「まず動かす」を置き、注意書きと詳細を後ろへ送る。LICENSE は参照3リポジトリにも前例が無い。決済コードを含む公開サンプルという性質から、ユーザー判断で MIT・著作権者 `k-ibaraki` を選定した。2026-09-05 ユーザー決定（grill-me） |
| 39 | 支払い主体の二重化（決定28 の⑤残論点）は「利用者ごとの PaymentSession」で解く | ウォレット（PaymentInstrument）と Payments 側の `userId`（`PAYMENTS_USER_ID`）はアプリで 1 つのまま、PaymentSession を利用者（Cognito の sub）ごとに切る。KVStore `payment-session` のキーを sub にし、`PAYMENT_SESSION_MAX_USD` / `PAYMENT_SESSION_MINUTES` は「利用者 1 人・1 セッションあたり」の値に読み替える（変数は増やさない）。「誰が支払わせたか」はセッション ID と sub の対応（KVStore の記録と作成ログ）で追う。`PAYMENT_SESSION_MINUTES` の検証は合成時・実行時とも「15 以上の整数」に強め、下限の定数は `payments/payment-session.ts` に 1 つだけ置いて両方から参照する。利用者ごとの instrument と WalletHub 委任は採らない | 解きたい問題は「利用者ごとの支出上限」と「誰が支払わせたかの監査」であり、依頼文が前提にしていた「利用者ごとの instrument と WalletHub 委任」は手段の一つに過ぎない。利用者ごとのウォレットは、利用者ごとに Coinbase 側の委任を人が行う前提と、instrument の作成・委任待ちの状態管理をアプリに持ち込む。**実測（2026-09-05）**: 同じ `userId`（`sample-user-1`）の下で PaymentSession を 2 本同時に作れ、どちらも `GetPaymentSession` で見えた。別の `userId` でも作れたが、その `userId` での `ProcessPayment` が他人所有の instrument に通るかは実決済が要るため未検証で、Payments の `userId` はウォレット持ち主のままにする（実測済みの経路だけで組む）。同時に `expiryTimeInMinutes` の下限が 15（5 分は `ValidationException`）と判明。代償: Payments 側の監査ログには利用者が現れず、対応はアプリ側の記録に依る。利用者数 × 1 セッションの枠が同時に開くため、アプリ全体の累積はウォレット残高だけが上限（決定35 と同じ）。2026-09-05 ユーザー決定（grill-me） |
| 40 | 利用者ごとの依頼回数の上限（決定36 が対象外にしたレート制限） | buyer API の `sendMessage` を利用者（Cognito の sub）ごとに固定の時間窓で数え、上限を超えたら受け付けない（既定 10 回 / 60 分。`BUYER_RATE_LIMIT` / `BUYER_RATE_WINDOW_MINUTES` で変更。合成時に正の整数を検証）。記録は KVStore `request-count`（キー「sub/窓の開始時刻」、TTL は窓の終わり）。KVStore に加算命令は無いので、読んだ値を `ifValueEquals` の条件にして書く compare-and-swap で数え、競合したら読み直す（上限 5 回）。`resume`（承認待ちへの応答）は回数に数えない代わりに、承認待ちが実在する応答だけを通すガード（`interrupt-guard.ts`）を buyer API に置く | 決定39 で利用者ごとの支出上限は付いたが、それは支払いに至った依頼にしか効かない。支払いに至らない依頼（ツールを使わない会話、売り手の失敗、承認で止まる購入）でも Bedrock の費用は掛かり、管理者が作った利用者が連続発注する経路は決定36 の後も開いていた。何を防ぎたいかを問うた結果、ユーザーは「Bedrock の費用対策としての依頼回数の制限」を選んだ（ウォレット全体の累積上限は採らず、引き続き残高が担う）。既定の 10 回 / 60 分はセッションの期限（60 分）と上限（1.00 USD = 10 購入）に揃えたもの。固定窓にしたのは、スライディング窓に要る時刻の列を KVStore の 1 値に持つより単純で、境界での倍出し（最大 2 倍）はこの用途では許容できるため。**セルフレビューでの是正**: 当初は「`resume` は承認待ちがあるときしか呼べず回数が有界」を根拠に数えていなかったが、`bb-agent` の `resume()` は承認待ちの実在を検証せず、応答さえ渡せばジョブを投入してモデルを起動する（`agent.js` の `resume`）。上限に達した後も `resume` を繰り返せば費用が出る穴だったため、ガードで前提の方を成り立たせた。書き込みの条件も同レビューで是正した。記録を読めないのに `ifNotExists` が落ちたら条件を外す——本番の `get` は期限切れを `null` にするが実体は消さず、窓の長さを変えるとキーが過去の窓と一致しうるため（60 分の境界は 120 分の境界を含む）、そのままだと利用者の依頼が窓の終わりまで通らなくなる（決定35 改訂と同じ罠）。2026-09-05 ユーザー決定（grill-me） |
| 41 | 買い手の画面の見た目 | `agent-app/index.html` の素の `<style>` を `src/style.css` に移し、Pico.css v2（`@picocss/pico`、blue ビルド）を npm 依存として `@import` する。マークアップは Pico の作法（`main.container` / `article` + `header` / チャット入力は `role="group"`）に寄せ、テーマは淡いブルー系（面と枠線を淡く、白抜き文字が載る primary は `#2c6fb0` = 白に対して 5.24:1 で AA を満たす濃さ）。情報の構成と、`src/index.ts` が触る ID / クラス名（`msg` `interrupt` `purchase` `signed-in` 等）は変えず、デバッグ用の表示（直近コード・イベントログ・会話 ID）もそのまま残す（見た目のための要素とクラスは変わる。`div.section` は `article` + `header` に、リード文は `hgroup` に置き換わり、`.section` は無くなった） | 動作確認用の素朴な画面のままでは利用者に分かりづらい、というユーザーの申し出。見た目だけを整える方針をユーザーが選択したため、機能と情報設計には触れていない。CDN 直読みではなく npm 依存にしたのは、他の依存の管理方針に合わせ、オフラインでも動くようにするため。CSS を TypeScript から import せず `<link>` で読むのは、tsconfig に vite/client の型が無く `npm run build` の tsc が `.css` の解決で落ちるため。`data-theme="light"` を固定したのは、独自要素（吹き出し・イベントログ）の配色が明色前提で、Pico の `prefers-color-scheme` 自動切替に任せると暗色環境で白い箱に白い文字が出るため。暗色対応は本決定の対象外とした。独自の配色は `--app-*` の変数だけでなく `.success` / `.error` / プレビュー枠の背景にも直書きがあり、さらに iframe に注入される売り手の HTML 自体が明色前提なので、外枠だけ暗色にしても揃わない。Authenticator（`@aws-blocks/auth-common`）は自前のカード枠を inline style で描くので、外側の `article` と二重にならないよう `!important` で打ち消している（当たらなくなっても二重枠に戻るだけで機能には影響しない）。2026-09-05 ユーザー決定（grill-me）。**追記（2026-09-06）**: 「情報の構成には触れない」は決定44 で上書きし、二段組と折りたたみに組み直した |
| 42 | ウォレット残高の画面表示（決定28 追記②） | 残高は AgentCore Payments の `GetPaymentInstrumentBalance`（chain `BASE_SEPOLIA`、token `USDC`）で取る（`payments/wallet-balance.ts`）。buyer API の `getWalletStatus` が残高・現在の PaymentSession の上限と残枠（`GetPaymentSession` の `limits` / `availableLimits`）・次のセッションの上限を返し、画面の「ウォレットと支払いの枠」に出す。残高の推移はブラウザ側で取り直すたびに値が変わっていれば行を足す（サインイン時・購入の tool-result / done・更新ボタン）。必要な追加は環境変数 `PAYMENT_CONNECTOR_ID`（`payments-setup.ts` の出力に加えた。無くても実決済は通るため必須にしない）と IAM `bedrock-agentcore:GetPaymentInstrumentBalance` | 候補は①Payments の `GetPaymentInstrumentBalance` ②viem で Base Sepolia の RPC に `balanceOf`（決定28 の下調べで実測した経路）。AgentCore Payments の機能を試すのが本サンプルの趣旨で、RPC の依存を増やさない①を採った。`aws bedrock-agentcore` CLI で残高やセッションを引けないのは `X-Amzn-Bedrock-AgentCore-Payments-User-Id` ヘッダーを渡せないためで、SDK は `userId` 引数から付けるので Lambda からは問題ない。API の `amount` が最小単位の整数か十進表記かは SDK の型では分からず、`formatTokenAmount` は両方を受ける（小数点を含めばそのまま）。実測で確定させる。IAM のリソースは既存の `payment-manager/*` に相乗りしており、残高取得がこの形で通るかは sandbox で確かめる。2026-09-06 ユーザー決定（grill-me）。**追記（2026-09-06）**: 依頼の枠の帯への表示と、購入中（`tool-call` から）の取り直しは決定47 |
| 43 | 支出上限を利用者が画面から変えられるようにする（決定39 の枠の操作） | 利用者（Cognito の sub）ごとの上限を KVStore `spend-limit`（TTL 無し）に持ち、無ければ環境変数 `PAYMENT_SESSION_MAX_USD` の既定を使う（`payments/spend-limit.ts`。正の金額・小数 2 桁までを小数 2 桁に正規化して保存）。`paymentSessionSource` の `maxSpendUsd` は関数でも受け、作成のたびにその時点の値で切る。buyer API の `setSpendLimit` は保存の後に現在のセッションを `DeletePaymentSession` で消し（`discardPaymentSession`。記録も消す。AgentCore 側で既に無い場合も成功、それ以外の失敗は記録を残して投げる）、次の購入で新しい上限のセッションが切られる。天井は設けない。変更できるのは自分の枠だけ | AgentCore Payments にセッションの上限を後から変える API は無く（`UpdatePaymentSession` は SDK に存在しない）、上限は作成時に固定される。ゆえに「画面で上限を変える」は「次に切るセッションの `maxSpendAmount` を変える」ことで、即時に効かせるには現在のセッションを消すしかない。作り直しは眼前の購入（`renew`）でも起きるため上限を作成時に解決する形にし、購入開始時の値で古い上限のセッションが切られないようにした。天井を置かない選択はユーザー決定で、利用者は管理者が作る（決定36）前提のもと、累積の上限は引き続きウォレット残高が担う（決定35・39 と同じ）。代償: 決定39 の「利用者ごとの支出上限」は利用者自身が外せる枠になり、他人に対する上限ではなく自分の支出を区切る仕組みとして読む。破棄は AgentCore 側を先に消してから記録を消す順にした。記録だけ消すと旧上限のセッションが AgentCore 側に生き残ったまま新しいものが切られ、枠が二重に開くため。2026-09-06 ユーザー決定（grill-me） |
| 44 | 買い手の画面の情報設計（決定41 の「情報の構成には触れない」を上書き） | 一枚のページを、ヘッダー（題名・サインイン中の利用者・サインアウト）→ 未サインイン時の説明とサインインカード → サインイン後の二段組（左: 「エージェントへの依頼」と「ウォレットと支払いの枠」を縦に、右: 「購入したページ」の一覧とプレビュー）→ 「内部情報（開発者向け）」の `details`（会話 ID と Realtime のチャンク。既定で開き、閉じた状態だけ localStorage に覚える）に組み直す。タブ部品は使わない（**この点は決定50 で上書きした。買えたページを会話の中に描くようにした結果、右列の「購入したページ」を会話とは別の面へ移す必要が生じ、画面内タブ 2 枚（依頼 / 購入履歴）に組み直した。二段組の中身も変わり、依頼タブは左＝会話、右＝「ウォレットと支払いの枠」になった**）。「新規会話」は送信の隣から依頼区画の見出し行の右端へ移し、吹き出しが 1 つでもあれば `window.confirm` で確認する。「更新」も同様にウォレット区画の見出し行へ。Authenticator は 1 つの要素をサインインの前後でカードとヘッダーの間で移し替えて使い回し、サインイン後は「Signed in as」の見出しを隠して `#auth-status` の日本語表示に置き換える。ローカル mock 専用の「直近のコードを表示」はサインインカードの中にだけ残す。コンテナ幅は 960px から 1280px に広げ、992px 未満で一列に戻す。DOM に依存しない規則（確認の要否・折りたたみ状態の読み書き）は `src/ui-rules.ts` に切り出してテストで固定。`src/index.ts` が触る ID・クラス名と、useChat・購入一覧・ウォレットの配線は変えない（**この点も決定50 で上書きした。購入一覧のコンテナを `#purchases` から `#history` へ改め、`#tab-chat` / `#tab-history` / `#panel-chat` / `#panel-history` / `#chat-status` / `#history-refresh-btn` を新設し、購入一覧の配線は会話の中のカードへ移した。useChat とウォレットの配線は変えていない**） | ユーザーの申し出（セクションの区切りが分かりにくい、送信の隣に新規会話があるのはおかしい、全体を見直したい）。2026-09-06 grill-me で決めた点: 想定利用者はデモを見る第三者と開発者の両方で画面は同一、デバッグ情報は出すが目立たせない。「今何が起きているか」の状態表示は本決定の範囲外（別途）。依頼と購入結果は一続きの流れ（`tool-result` で購入一覧が自動更新される）なので、タブで分断せず二段組で同時に見せる。プレビューは右列の一覧の下（選んだものがすぐ下に出る）。内部情報は自前のタブではなく `details` にしたのは Pico が素の要素を整えてくれ、キー操作や aria を自分で面倒見なくてよいため。新規会話は押し直しが効かない（画面から再開する手段が無い）ため会話があるときだけ確認する。決定42・43 のウォレット区画は本決定の作業中に別ブランチで足されたもので、支払いに関わる情報として依頼の下（左列）に置き、成果物（右列）と分けた。ユーザー決定（grill-me）。**追記（2026-09-06）**: 残高と残枠は依頼の枠の中の帯にも出す（決定47） **追記（2026-09-07、決定51）**: 依頼タブの二段組もやめた。「ウォレットと支払いの枠」は 3 枚目のタブへ移り、どの面も区画 1 枚の一列になった |
| 45 | agent-app のフロント配信は既知のファイルだけを返し、未知のパスは 404 にする | Amplify Hosting: コンソールがアプリ作成時に自動で付ける SPA 用の規則 `/<*> → /index.html (404-200)` を外し、代わりに `/<*> → /404.html (404)` を置く（`aws amplify update-app --custom-rules`。アプリ単位の設定でリポジトリでは管理できないため README に手順を書く）。体裁用の `agent-app/public/404.html` は外部の CSS・JS を読まない自己完結の一枚。ローカル（`npm run dev` / `vite preview`）も `vite.config.ts` の `appType: 'mpa'` で同じく 404 にする。CDK 直 deploy（決定33 の退路）は `aws-blocks/index.cdk.ts` の `Hosting` に `framework: 'static'` を明示して同じく 404 にする | ユーザーの申し出（任意のパスでもアプリが動いてしまう。購入した HTML に細工されたときの露出を減らしたい）。原因はコードではなく配信層で、Amplify では上記の自動規則、ローカルでは Vite 既定の SPA フォールバック。購入 HTML は `sandbox="allow-scripts"` + `srcdoc` の不透明オリジンで描画しているので（決定29）このフォールバック自体が権限を増やすわけではないが、任意 URL が本体を 200 で返すのは不要な露出であり、この画面はクライアントルーティングを使わないので締めても失うものが無い。Amplify の規則はアプリ単位の手動設定でコードにもテストにも現れないため、アプリを作り直すとコンソールが再び `404-200` を付けて保護が黙って消える。`amplify.yml` から `update-app` を冪等に流す案は、サービスロールに `amplify:UpdateApp` が要りビルドがアプリ設定を書き換える副作用を持つため採らず、README の手順による手動運用とする（この残存リスクは承知の上）。**当初の記述の訂正（同日、セルフレビュー）**: 初版は「Blocks の CDK 直 deploy（`Hosting`）は `spaFallback` 既定 false で元から未知パスは S3 の 403/404 になるため変更なし」としていたが誤りだった。根拠にした `?? false` は CloudFront Function を組み立てる低層（`@aws-blocks/hosting` の `defaults.js`）の既定で、実際にそこへ渡る値はアダプタが決める。`detectFramework` は next / nitro / astro / sveltekit のいずれでもない構成を全て `'spa'` に落とすため（`adapters/index.js`）、この Vite プロジェクトは `'spa'` 判定になり `getAdapter` が `spaFallback: true` を渡す。よって `framework: 'static'` の明示が要る。`'static'` にすると SPA アダプタが `dist/404.html` の存在を見て `errorPages[404]` に自動で配線するので（`adapters/spa.js`）、追加した `public/404.html` がそのまま効く。`DEFAULT_BUILD_DIRS.static` も `'dist'` で既存の `buildOutputDir` と矛盾しない。2026-09-06 ユーザー決定 |
| 46 | チャット欄の Agent 応答を Markdown で描く | Agent（`role === 'assistant'`）の吹き出しだけを Markdown として描画する。`src/markdown.ts` の `renderAssistantMarkdown()` が marked（`gfm: true` / `breaks: true`）で HTML にし、DOMPurify（既定の設定）に通してから `innerHTML` へ渡す。利用者の吹き出しと承認待ちの表示は従来どおり `textContent` のまま。生成中（`onLoadingChange` の loading が真の間の末尾のメッセージ）は素の文字列で出し、生成が終わった時点で Markdown に描き直す。CSS は `.msg.markdown` で `white-space` を `normal` に戻し、吹き出し内のブロック要素の余白と見出しの大きさを整える | ユーザーの申し出（Agent の応答が箇条書きや見出しを含んでも素のテキストで出て読みにくい）。`src/index.ts` は「文字列は必ず textContent で入れ、innerHTML は使わない」を方針にしていたので、その例外をここ 1 か所に限り、サニタイズを代わりの防護に据えた。LLM の出力は信頼できない入力（売り手の応答や利用者の入力が混ざる）なので、marked に生 HTML を通させたうえで DOMPurify が落とす形にし、自前の許可リストは書かない。`breaks: true` は、`pre-wrap` をやめた後も単独の改行が改行として見える状態を保つためで、既定の `false` だと一段落に潰れて素のテキストで出していた頃より読みにくくなる。生成中に素の文字列で出すのは、`bb-agent` の `useChat` が `text-delta` ごとに `onMessagesChange` を呼ぶため（`index.hooks.js`）で、閉じていない ``` を解釈すると後続が丸ごと消えては戻る点滅になる。その「生成中の吹き出し」は末尾ではなく最後の assistant で判定する（`ui-rules.ts` の `findLastAssistant`）。`respondToInterrupt` は承認の吹き出しを末尾に積んだうえで、既存の空プレースホルダがあればそれを生成先に再利用するため、末尾の決め打ちでは外れるため。変換の結果は本文をキーに覚える（再描画が delta のたびに走り、そのつど過去の応答まで変換し直すのを避ける）。見た目はブラウザでの目視確認に委ねる（Vite に一時ページを立てて確認し、削除）。**セルフレビューでの是正**: 当初は「生成中の吹き出し＝末尾」で実装し、単体テストも置かなかった（jsdom を足しても DOMPurify 自体を試すことになる、という理由）。前者は上記の承認経路で外れる誤りだったため最後の assistant に改め、後者は TDD の規約に対して弱いため jsdom を devDependency に足し、`markdown.test.ts`（このファイルだけ `@vitest-environment jsdom`）で「何が通り、何が落ちるか」——見出し・箇条書き・表・`<br>`・通常のリンクは残り、`<script>`・イベントハンドラ属性・`javascript:` の href は落ちる——を固定した。2026-09-06 ユーザー決定 |
| 47 | 依頼の枠に残高と残枠の帯を出し、購入中に取り直す（決定44 の左列の構成と決定42 の取り直しの契機を一部上書き） | 「エージェントへの依頼」の見出し行の直下に帯を常時表示し（**決定50 で画面をタブに分けてからは、帯は依頼タブの中にあり、購入履歴タブを開いている間は見えない。購入中の取り直しは続く**）、「残高 ○○ USDC」「残枠 ○○ USD」の 2 つだけを大きめの数字で出す（説明文は付けない。今のセッションが無ければ残枠は「—」。**この「—」は決定49 で上書きし、セッションが無いだけのときは次に切る上限を出すようにした**）。値が変わった瞬間に数字の背景を一瞬光らせて数秒で戻す（CSS アニメーション。`prefers-reduced-motion` でも止めない。ただし「—」との出入りでは光らせない。残高 API の失敗を「減った」と見せないため。**決定49 で判定を文字列から金額に変えたが、狙いは同じ**）。有料ツール（`generateHtml`）の `tool-call` チャンクを受けたら 3 秒間隔で残高と残枠を取り直し、値が変わるか `tool-result` / `done` / `error` が届くか、`BUYER_TOOL_TIMEOUT_MS` の既定と同じ 600 秒に達したら止める（取得の失敗はその回を飛ばして続けるが、上限の判定は取得の成否によらず通す。`interrupt`（決定31 の再購入確認。中断中は `done` も `tool-result` も出ない）・ストリーム自身の失敗（`onError`）・新規会話・サインアウトでも止める）。**止める判定に使うのは残高だけで、残枠は混ぜない**（残枠は署名の時点で先に減り、セッションが無い状態からの購入では「—」から数値に変わるため、混ぜると残高が減る前に止まる）。残高が取れなかった回は「動いていない」とみなして続ける。見えていないタブでは叩かない（`document.visibilityState`）。残高の取得は更新ボタン・取り直し・購入後の再取得が並行し得るので通し番号を持ち、追い越されて遅れて返った古い応答は画面にも推移にも反映しない。サインアウト・利用者の切り替えでは通し番号を進め、飛行中の応答が前の利用者の残高を描き戻さないようにする。既存の「ウォレットと支払いの枠」はそのまま残し、帯と同じ取得結果（`getWalletStatus`）を共有する（「残高の推移」にも今まで通り行が足される）。DOM に依らない規則（帯の文字列・取り直しの開始条件と継続判定・変化の判定）は `src/ui-rules.ts` に置きテストで固定 | ユーザーの申し出（実決済で試したところ、依頼の枠と別の枠にある残高は目に入らず、減ったことが伝わらない。依頼の枠の中でパッと見で分かるようにしたい。細かい説明は要らない）。grill-me で確かめた事実: 実決済では支払いの時点で画面の残高は減らず、成果物が届いた時点（`tool-result` / `done` の取り直し）で減った。支払いは有料ツールの呼び出しの中で起き、ツールが返るまでブラウザには何も届かないため、「支払った瞬間に減る」には呼び出し中の取り直しが要る（ユーザー決定）。取り直しは値が変わったら止めるので、1 購入あたりの AgentCore Payments の API 呼び出しは数回〜十数回増える。スロットリングの挙動は未実測で、実決済で確かめる。減った瞬間の見せ方は「一瞬光らせる」を採り、差分の表示は付けない（ユーザー選択）。**追記（2026-09-06、セルフレビュー）**: 当初は `prefers-reduced-motion` で光らせない実装にしたが取り止めた。位置の移動を伴わない色の変化で前庭系への刺激に当たらず、消すとその利用者にだけ「減った」合図が何も残らないため。通し番号・上限判定・`onError` での停止も同レビューでの是正で、いずれも「支払ったのに減って見えない」「取り直しが永久に止まらない」を防ぐもの。**追記（2026-09-06、PR #18 のレビュー）**: 初版は停止の判定に残高と残枠の組を使っており、残枠が先に動くため「支払った瞬間に残高が減る」がほぼ全ての購入で成立しなかった（購入のたびに最初の 3 秒で止まる）。判定を残高だけに変えた。同じ組の比較が、サーバーが正常応答で返す残高取得の失敗（`balance: null`）も「変化」と扱い、「—」を減少と同じ点滅で見せたうえ監視を打ち切っていたのも同根。`interrupt` の取りこぼしは、承認待ちの間ポーリングが続く（最悪 200 周・課金 API 約 400 回）ため停止契機に加えた。ポーリングの総量は当初「1 購入あたり数回〜十数回」と見積もったが、上限まで回れば 200 周・API 約 400 回になる。既存区画を残すのもユーザー選択で、帯は要約、区画は詳細と操作という役割分担になる。2026-09-06 ユーザー決定（grill-me） **追記（2026-09-07、決定51）**: 帯は依頼タブに残したまま、詳細の区画だけを「ウォレット」タブへ移した。帯の取り直し（`tool-call` からのポーリング）は面の切り替えと無関係に動く。停止の判定に使う `document.visibilityState` はブラウザのタブであって画面内の面ではないため、ウォレットの面を閉じていても取り直しは止まらず、`hidden` の面のノードにも値は届く |
| 48 | 二重支払いの防護が届いていなかった範囲（決定31 の適用範囲の是正） | ①未解決の購入の記録を利用者（Cognito の sub）ごとに KVStore `unresolved-payment` に持ち、承認の要否は会話履歴（この会話）と記録（この利用者）の**和**で判定する（`repurchase-guard.ts` の `pendingApprovals`）。記録は購入が成功したときだけ消し、承認だけでは消さない。TTL は付けず、上限 20 件で古いものから落とす ②`ProcessPayment` の失敗を「支払いが立っていないと言い切れるもの」と「言い切れないもの」に分け、後者を `UncertainPaymentError` として区別する。言い切れるのはサービスが 4xx で拒んだ場合（AWS SDK の `$fault` が client。支出上限の超過・セッションの失効を含む）で、タイムアウト・接続断・5xx・応答に支払い証明が無い場合は言い切れない。成否不明の購入もレシート（`paymentUncertain`）を残して①の未解決に数え、画面には「支払いの成否不明」と出す | ユーザーの依頼「全体を見直して致命的な不具合が無いか確認する」で見つけた 2 件。①決定31③の判定は会話履歴だけを見ていた。ウォレットと支払いの枠は利用者ごとで会話をまたぐのに（決定39・43）、防護だけが会話に閉じていたため、事故の残る会話を離れて新しい会話を作れば承認を経ずに買い直せた。画面の「新規会話」を押すだけで防護が外れる状態で、決定31 が置いた人の承認は実効を失っていた。②決定31②が `PaidToolError` にしていたのは「支払い証明を売り手へ送った後の例外」だけで、`ProcessPayment` 自体の失敗は `x402-payer.ts` の注記どおり「署名前なので二重にならない」と見なしていた。しかしクライアント側のタイムアウトは、応答が届かなかっただけで AgentCore 側の処理は済んでいた、という筋を否定できない。この失敗はレシートも承認要求も無いまま LLM へ「失敗」として渡り、決定30 の冪等キーは購入のたびに採番し直される（`buyer-agent.ts` の `resultId`）ため、買い直しは別の `clientToken` になって Payments 側の冪等性も効かない。SDK の既定の再試行（`maxAttempts` 3）は同じ `clientToken` で行われるので安全であり、穴が開くのはそれを使い切った後である。判定を例外の名前の列挙ではなく「4xx か否か」に置いたのは、未知のサービス例外を取りこぼさないため（取りこぼしたときは成否不明の側＝安全側に倒れる）。代償: タイムアウトの起きた購入は、実際には支払われていなくても次の購入で人の承認を求める。決定31 が「毎回の承認を必須にする案は自律性を弱めるため不採用」とした線からは承認寄りに動くが、金が動いた可能性のある場合に限る。会話単位の判定（決定31 の「最後に成功した購入より後」）はそのまま残し、利用者単位の記録が書けなかったときの備えとして二重にする。**この二重化を成り立たせるため、記録の書き込みが失敗してもツールは要約（`resultId` と `paymentMade` / `paymentUncertain`）を返して終わる**。投げて終わると tool-result が会話に残らず、KVStore の記録と会話単位の防護が同時に失われるため。書き込みは防護の記録を先にし、レシート（`purchased-html`）を後にする（片方しか残らないなら、残すべきは次の支払いを止める側）。記録に TTL を付けないのは、期限切れで防護が黙って外れるのを避けるため（`payment-session` ではなく `spend-limit` に倣う）。限界: 購入の成功は、その間に別の購入が書いた未解決も一緒に消す（`clearUnresolved` は読んだ記録を条件に消すため）。決定31 の「最後に成功した購入より後」の意味論と同じ振る舞いで、消えた分は会話単位の判定が拾う。実装は単体テストで固定したが、**実決済での確認は未実施**。2026-09-06 |
| 49 | セッションが始まる前の残枠を帯に出す（決定47 の「セッションが無ければ—」を上書き） | `getWalletStatus` の結果全体から帯の残枠を決める（`src/ui-rules.ts` の `stripRemaining`）。①今の PaymentSession があればその残枠 ②セッションが無く、取得にも失敗していない（`sessionError` が無い）ときは、次に切るセッションの上限を `1.00 USD（セッション開始前）` の形で出す ③セッションの取得に失敗した回と、セッションはあるが残枠が返らない回は従来どおり「—」。光らせる判定は表示文字列ではなく金額（`StripCell.value`。取れていなければ `null`）で行い、`null` との出入りでは光らせない | 上限を変えると現在のセッションを破棄する（決定43）ため、変更してから最初の購入までの間は必ず `session: null` になり、帯の残枠が「—」のまま何も分からなかった（ユーザー申告）。セッションを先回りして切ることはしない（決定35 の「購入のときに切る」を崩し、購入前に支払いの枠を開いてしまうため）。`session: null` は「まだ無い」ときと「取得に失敗した」ときの両方で返るので、後者で上限を出すと失敗が実値らしく見える。そこを `sessionError` で分けることで、決定47 の「失敗を減少と見せない」を保っている。添え書きは付けない案もあったが、実値と見分けが付かないためユーザーが「（セッション開始前）」を選んだ。判定を金額に移したのは、添え書きが外れただけの回（`1.00 USD（セッション開始前）` → `1.00 USD`）で光らせないため。2026-09-06 ユーザー決定 |
| 50 | 買えたページは会話の中に描き、購入履歴は利用者ごとに会話をまたいで一覧する（決定44 の情報設計と決定29 の描画位置を一部上書き） | 画面を画面内タブ 2 枚に分ける。①依頼: 左に会話（帯を含む）、右にウォレットと支払いの枠（`3fr : 2fr`）②購入履歴: 会話をまたいだ購入の一覧と、選んだ 1 件のプレビュー。買えたページは会話の中のカード（`purchase-card`）に描き、カードごとに `mountPreviewHost` で View を 1 つ載せる。自動で載せるのはストリームで届いた購入（その場で買えたもの）だけで、再開した会話の買い置きと履歴の行は「表示」を押したときに載せる。会話の描画は `replaceChildren` をやめ、メッセージ ID を鍵にした差分更新にする（並びの規則は `src/ui-rules.ts` の `orderChatNodes`。カードは届いた時点の末尾の吹き出しを錨に持つ）。購入履歴は buyer API の `listPurchaseHistory` が `listConversations(userSub)` で所有を解決し、新しい順に会話の履歴を読み、購入のある会話を最大 20 件集める（`aws-blocks/purchases.ts` の `purchaseHistory`。5 会話ずつ読み進め、購入のある会話が 20 件そろうか、読んだ会話が 100 件に達するか、会話が尽きたら止める。1 会話の取得失敗は飛ばし、読んだ数・総数・失敗数を返して画面が読み残しを伝える）。呼ぶのは履歴タブを開いたとき・「更新」のとき・会話の中の購入が増えたときに限る。新規会話では会話の画面だけを捨て、履歴は残す（サインアウトでは履歴も捨てる） | 新規会話を押すと、それまでに買ったページが画面から辿れなくなっていた（ユーザー申告）。購入物そのものは利用者ごとの KVStore（`purchasedHtmlKey(userSub, resultId)`）にあり、`getPurchasedHtml` は会話に依存しないので、失われていたのは索引だけだった。索引を利用者単位にする方法として KVStore の `scan` は全利用者ぶんを読むため採らず、所有の解決にそのまま使える `listConversations` を選んだ。会話をまたいだ一覧は会話の履歴を丸ごと読むので、たどる会話は 20 件までに抑え、購入のたびには呼ばない（会話の中のカードが増えたときだけ古い扱いにする）。**追記（2026-09-07、PR #21 のレビュー）**: 初版は読む会話を 20 件で切ってから購入のある会話に絞っていたため、購入の無い会話（新規会話を押して 1 度送れば残る）が 20 件たまると、その前の購入が黙って消えた。上限を「返す会話の数」に改め、読む数の天井（100 件）を別に置き、`Promise.all` を `allSettled` にして 1 会話の失敗で全体を落とさないようにした。同じレビューで、会話の中の購入の取り直し（`refreshPurchases`）と購入履歴（`refreshHistory`）に世代・通し番号の確認が無く、会話を捨てた後やサインアウト後に遅れて返った応答が前の会話・前の利用者の購入を描き戻す穴も塞いだ。セッション開始前の残枠は光らせる判定に乗せない（上限の変更でセッションを破棄しただけで光っていた）。描画位置を右列から会話の中へ移したのはユーザーの申し出（MCP Apps なのだからチャットの中に出したい。横幅も広く使える）。差分更新にしたのは、`replaceChildren` で毎回組み直すと会話の中の iframe が読み込み直しになり、`AppBridge` の接続が切れるため。再開した会話で自動描画しないのもユーザー決定（会話の履歴そのものを追う機能は持たず、成果物は購入履歴から開ける）。決定31 の再購入ガードは会話単位のまま（利用者単位にすると、未解決の支払いが以後すべての会話を止める）。**追記（2026-09-07、PR #21 の 2 巡目のレビュー）**: 面を分けた副作用として、承認の操作（`#interrupts`）が依頼の面の中に入り、購入履歴の面を開いたまま承認待ちになると画面から辿れなくなっていた。中断中は `done` も `tool-result` も届かず帯の取り直しも止まる（決定47）ため、履歴の面は静止したままになる。`renderInterrupts` で承認が来たら依頼の面へ引き戻す。また面の切り替えは `role="tablist"` / `role="tab"` を名乗るのをやめ、`aria-pressed` の素のボタンにした。ARIA のタブは矢印キーでの移動と roving tabindex があるものとして案内されるが、それを自前で持つのは決定44 が `details` を選んだ理由（キー操作と aria を自分で面倒見なくてよい）に反するため。`showCardPreview` は `showHtml` を待つ間に会話を捨てられると `PreviewHost` を閉じ損ねていた（`card.host` への代入が待ちの後にあり、後始末はまだ null を見る）ので、待った後にもう一度世代を確かめる。表示の失敗も例外文を画面に出さず内部情報のログへ回した（`refreshHistory` と同じ扱い）。2026-09-06 ユーザー決定 **追記（2026-09-07、決定51）**: タブは 3 枚（依頼 / ウォレット / 購入履歴）になり、①の依頼の面は会話 1 枚の一列になった |
| 51 | ウォレットと支払いの枠を 3 枚目のタブへ移す（決定50 のタブ構成と決定44 の二段組を上書き） | 画面内タブを 3 枚にし、並びは 依頼 / ウォレット / 購入履歴。「ウォレットと支払いの枠」の `article` を `#panel-wallet`（`#tab-wallet`）へ移し、依頼の面は会話 1 枚の一列にする（`.workspace` の二段組 CSS は削除し、クラスも外す）。残高と残枠の帯（決定47）は依頼の面に残す。`showTab` が扱う面は `TAB_NAMES`（`chat` / `wallet` / `history`）に広げ、切り替えの配線もこの配列から回す。ウォレットの取り直しの契機は変えない（サインイン時・購入中のポーリング・`tool-result` / `done`・更新ボタン）。コンテナ幅は 1280px のまま | ユーザーの申し出（購入履歴と同じく、ウォレットと支払いの枠も別の面にしたい）。並びは使う頻度の順でユーザーが選んだ。購入履歴の `historyStale` に当たる仕組みは持たせていない。`hidden` はノードを消さないので `refreshWallet` の書き込みは隠れた面にも届き、決定47 のポーリングが見る `document.visibilityState` はブラウザのタブであって画面内の面ではないため、既存の契機だけで面は最新に保たれる。幅を 1280px から狭めなかったのは、決定50 の「買えたページを会話の中に描くので横幅も広く使える」を保つため（決定44 が 1280px にした理由は二段組だったが、会話の中の iframe が幅を引き継ぐ）。既知の帰結として、支出上限を入力している最中に承認（interrupt）が来ると決定50 の引き戻しで依頼の面へ移る（入力値は消えず、位置だけを見失う）。支払いを続けるかどうかの判断を待たせない方を優先し、機構は足さない。2026-09-07 ユーザー決定 |
| 52 | 公開前の点検で記録に残す情報の線引き | リポジトリは将来公開する前提で、①実環境の識別子（無認証エンドポイントの Function URL・ロググループ名・オンチェーンのトランザクションハッシュ）は記録や PR 本文に書かず、既にあるものはホスト部分やハッシュを伏せる ②コミットの author メールアドレスは秘匿情報ではないとして許容し、履歴の書き換えはしない ③`agent-app/.blocks/config.json` は gitignore せずコミットしたままにする | 2026-09-07 の公開前点検（追跡ファイル・git 全履歴・PR / Issue / コメント本文・構成図を走査）で、秘密鍵・API キー・AWS アカウント ID・メールアドレス・ホームパス・Cognito / Amplify の ID は見つからなかった。残ったのが①の 3 種で、URL は既に 403 を返す死んだものだが、無認証エンドポイントを記録に置く習慣自体を避ける。tx ハッシュはテストネットでもエクスプローラからウォレットアドレスに辿れる。git 履歴と PR 本文の編集履歴には残るが、ユーザーは履歴が残ることを許容した。②はユーザー判断。③は `@aws-blocks/core` の `getStackName` が「コミット済みの `.blocks/config.json`」から `stackId` を読み、CDK 直経路のスタック名 `<stackId>-prod` / `<stackId>-<sandboxId>` を決めるため（無いと `npm run deploy` / `sandbox` / `destroy` が合成前に落ちる。公式スキャフォールド `create-blocks-app` の gitignore も除外していない）。中の `telemetry.projectId` は匿名の乱数 UUID で、公開しても差し支えない。ユーザー決定。**追記（同日）**: ③の帰結として、fork 先で CDK 単独 deploy を使う場合に `stackId` を書き換える注意を `agent-app/README.md` の「運用上の注意」に書いた（スタック名 `<stackId>-prod` と Agent 内蔵の S3 バケット名がここから決まり、S3 名は全アカウントで一意のため。sandbox は `.blocks-sandbox/sandbox-id.txt` の乱数付きで衝突せず、Amplify 経路はこのファイルを読まない）。あわせて README と CLAUDE.md の「CDK 直」という内部語彙を「Amplify を介さず AWS CDK だけで deploy する経路（CDK 単独 deploy）」に言い換えた。ユーザーが Bedrock の機能と読み違えたため（決定38 の方針どおり、読者向け文書から内部語彙を外す）。本ファイルと implementation-log.md の過去の記述、コードコメントは据え置く。ユーザー決定。**訂正（同日、セルフレビューとユーザー指摘）**: 直上の言い換えは二重に誤っていた。①理由を「ユーザーが Bedrock の機能と読み違えたため」と書いたが、Bedrock はユーザーが「こういう意味か」と挙げた候補であって、確定した理由ではない。CDK と Bedrock は無関係で、この一行は決定録として意味を成さない。担当（Claude）がユーザーの推測を確定した理由として書き込んだもので、決定38 の訂正1 と同型の誤り。②「直」を「単独」に替えただけで、読者の疑問には何も答えていなかった。真の欠落は「AWS Blocks で書いているのに、なぜ CDK が出てくるのか」が何処にも書かれていないことだった（ユーザー指摘「AWS Blocks を使っているのに CDK 直だと分からない」）。AWS Blocks は Block の宣言から CDK の構成を生成するので、AWS へ載せる作業はどの経路でも `cdk` の合成と deploy になり、違うのは誰がそれを走らせるかだけである（Amplify のビルドが `ampx` 経由で走らせるか、手元から `cdk deploy` を叩くか）。この説明を `agent-app/README.md` の「アーキテクチャ」と「クラウド deploy」の頭に足した。呼び名は既存の「Amplify 経路」と対になる「`cdk deploy` 経路」に統一し、README・CLAUDE.md に加えて `runtime-env.ts` が投げるエラーメッセージとコードコメントまで揃えた（エラーメッセージは deploy した人の画面に出る文言で、コードコメントとは読み手が違う）。旧称「CDK 直」が残るのは、本ファイルと implementation-log.md で当時の判断や発言を書き留めている箇所だけで、そこは記録なので書き換えない。ユーザー決定 |
| 53 | 売り手の中で判断・見積もりに使うモデル | Bedrock の Haiku 4.5 を使う。TypeSafe AI の Jev（System One Model）は採用しない | 2026-09-20 調査。Jev は型付きの判断だけを返すモデルで、入力 $0.042/1M・出力無料、応答 70〜500ms と確かに安く速い。しかし ①Bedrock で提供されず `api.typesafe.ai` か Vercel AI Gateway 経由になり、AWS 完結というサンプルの筋が崩れる ②買い手のプロンプトが AWS 外へ出る（リージョンもデータ取り扱いも公式文書に記載を見つけられず）③早期アクセス（waitlist）のため読者が再現できない ④テキスト専用で添付（画像・PDF）を読めず、出力量を左右する要素が判定器から見えない ⑤日本語での判定品質が未検証 ⑥ベンチマークはベンダーの自己計測で、開発元自身が「価格が補助されていないことは証明できない」と注記している。Haiku なら1回あたり $0.001 前後・1秒程度で、$0.1 の取引と 600 秒の処理予算（決定31）に対して Jev との差は誤差に収まる。AWS 内に留まる利得のほうが大きい **追記（2026-09-20、実測）**: ローカルで動く Jev 互換実装を 5 種類（LFM2.5-1.2B / ModernBERT-ja-310M / TinySwallow-1.5B / Qwen2.5-1.5B / Qwen3-1.7B）測った。主観ラベルに対する的中は、枠組みを五通り（日本語 choice・英語 choice・日本語 score・英語 score・Noul 分解）試して 5〜15/20。最良は Qwen2.5-1.5B の英語 score で 15/20（239ms）、二問の max を採る規則で 18/20 だが閾値ごと過学習。Haiku は同条件で 19/20（1,037ms）。所見として、①判定の形は choice より score が全基盤で優れる ②criteria を英語にすると英語基盤のモデルで大きく改善する（LFM の score が 7→11） ③日本語特化の蒸留（TinySwallow 12/20）が素の多言語基盤（Qwen2.5 15/20）を下回る ④思考モデル（Qwen3）は先頭トークンの確率を読む方式と噛み合わず測定不能 ⑤System One の枠組みは小型モデルほど有用で、素の生成に落とすと LFM は 6/20 まで落ち 20 件中 8 件で形式指示にすら従えなかった。決定は据え置き（Haiku を使う） **見直し（2026-09-21、TypeSafe の docs と突き合わせ）**: 現行構成のまま判定器を点検し、5 点を手当てした。①段の読み取りを完全一致から語単位の照合へ改めた。`take。` や `` `take` `` のような飾りで読めなくなり、黙って中央の段へ落ちて価格に響いていた。語の切れ目で拾い（`document` の中の `ume` は掴まない）、段が複数混じれば読めない扱いにする。併せて `maxTokens` を 8 から 16 へ上げ、前置きを添えられたときの語の切断を避けた。②Bedrock 側のプロンプトにだけ入っていた前置き（極端に短いページは出てこない）を `TIER_CONTEXT` として括り出し、System One 側の `instructions` にも渡した。両判定器の土台が揃っていなかった。docs が `instructions` にオブジェクトを許し、問いと前置きを別の欄に分ける書き方を推奨しているため、`{ question, context }` の形にした。③System One の応答で `response.ok` を見ていなかったため、鍵切れ・過負荷・応答形式の変更が同じ警告 1 行に潰れていた。HTTP ステータスを警告に載せる。④判定器が返す確信度を誰も読んでいなかったので、`ResolvedQuote` 経由で 402 のログに出す。判断には使わない。score は水準の期待値であり、判定が割れるほど値は中央へ寄って竹に着地するため、確信度の閾値分岐は要らない。⑤`scoreToTier` の境を水準数から導くようにした（3 段では挙動不変）。併せて `parseTierTable` が価格の順序と 0 ドルを検めていなかった穴を塞いだ（目安トークン数だけを見ていた）。見送ったのは、確信度による人手エスカレーション（無認証の自動売買で人が介在しない）、依頼文→段の短期メモ化（実測で `extra` は往復しており、ステートレスを濁す割に得るものが薄い）、Bedrock 経路の tool use 化（①の手当てで実害が消える） **訂正（2026-09-21、ユーザー指摘）**: 理由①の「AWS 完結というサンプルの筋が崩れる」は、記録者が置いた枠付けであってユーザーの前提ではなかった。ユーザーは AWS 完結にこだわっておらず、外へ出るかどうかはトレードオフ次第と考えている。理由①は判断の決め手ではないものとして読むこと（行は残す）。併せて現況を二点。理由③の早期アクセス（waitlist）は解消しており、コンソール（console.typesafe.ai/keys）から鍵を自取得できる。理由⑤⑥（日本語での品質が未検証／ベンチマークがベンダーの自己計測）は、2026-09-21 に本物の Jev を自前で測って解消した（数値は決定58）。**この決定自体は維持する**（既定は Haiku）。判定器を選べるようにする話は決定58 で扱い、決定53 を覆すものではない |
| 54 | 有料ツールの値付け（決定18 の 0.1 テスト USDC 固定を上書き） | 呼び出しごとに Haiku が見積もった額を**上限**として提示する。決済の器は `exact` と `upto` を accepts に併記し、買い手が署名できるほうを選ぶ。`upto` の買い手には実費に基づいて上限以下へ割り引いて決済し、生成に失敗したら `amount` を `"0"` として課金しない。`exact` の買い手には上限額を満額請求する。見積もりを超えた分は売り手が被る。割引の規則（トークン単価表）は先に公表し、ツール結果に実測トークン数と適用単価を添えて返す | 原価は出力トークン次第で $0.09 程度から、上限の 64,000 トークンまで出れば $1 近くまで振れるのに、売値は $0.1 固定だった。小さな依頼では取り過ぎ、大きな依頼では持ち出しになる。実費だけを請求する案は検討のうえ退けた。`upto` では買い手が使用量を検証できず、上限の内側で売り手が一方的に額を決める形になって対等でないため。買い手が署名前に確定した数字を見て買うか決められること（エージェントが値段を見て判断する、という本サンプルの見どころ）を優先し、見積もり違いは売り手が負う固定価格の規律を残す。`upto` は x402 の従量課金スキームで、README が「LLM のトークン生成のような変動費のエンドポイントに最適」と名指ししている。Base Sepolia での対応は x402.org の facilitator の `/supported` で実測（2026-09-20）。割引を口約束にすると、売り手が毎回上限どおり決済していても買い手は気づけないため、規則の公表と根拠の開示を条件に含める。懸念として、超過を売り手が被る以上は高めに見積もる誘因が働き、その安全余裕を `exact` の買い手が満額払うことになる。単価表の公表に加えて安全余裕の率に天井を設けるかは設計で詰める。ユーザー案（2026-09-20） **追記（2026-09-20、U10 の検証）**: AgentCore Payments は `upto` を署名できることを実測した。ただし署名の時点で PaymentSession の枠が上限額で消費され、割り引いても戻らない。したがってこの値付けは「買い手の財布は軽くなるが、利用者ごとの枠（決定39・43）は上限額のまま減る」性質を持つ。上限に安全余裕を積みすぎると、支払っていない額で枠を食い潰すことになるため、余裕の率には天井が要る **改定（2026-09-20）**: 「呼び出しごとに見積もった額を上限として提示する」という骨子は決定56 に置き換える。実測により依頼文から原価が予測できないことが分かったため、見積もりではなく段の判断と規模の制御で原価を決める。`exact` と `upto` の併記、割引と失敗時 0 決済、開示の条項は決定56 の下でもそのまま生きる **今回は見送り（2026-09-21、ユーザー判断）**: `upto` の併記は将来対応として繰り越す。理由は二つ。①`@x402/mcp` に決済額の上書きが無いため、支払い処理を上流のラッパーに任せず `x402ResourceServer` を直に駆動する自前の settle 経路が要り、お試し実装として重い。②決定21 の upfront も開け直しになる。今回は決定56（段の判定と規模の指示）を `exact` のみで実装した。U10（AgentCore Payments が `upto` を署名できる）の実測結果は将来の出発点として残す |
| 55 | 見積額を 402 応答と支払い呼び出しで一致させる方法（U11 の決着。決定54 の前提） | 見積書を HMAC で封じて `accepts[].extra.quote` に載せる。中身は 版・ツール名・引数のハッシュ（正規化 JSON の SHA-256）・見積額・有効期限を連ねたものと、それを HMAC-SHA256 で封じた値。支払いの無い呼び出し（402 を返す側）でだけ Haiku に見積もらせて封を作り、支払い付きの呼び出しでは Haiku を呼ばず、封を検めて同じ額で accepts を組み直す。鍵は Secrets Manager に置き、コールドスタートで一度読んでコンテナ内に保持する。有効期限は 15 分とし、切れた場合は再見積もり（買い手には新しい 402 が返る） | 上流の照合は `amount` を含む core フィールドを `deepEqual` で突き合わせ、`extra` は「売り手の要求が買い手の申告の部分集合であること」を求める（`dynamicExtraFields` を除く。`@x402/core` の `paymentRequirementsMatchAccepted` で確認）。したがって見積額を 2 往復で再現できなければ、買い手は 402 を受け取り続ける。Haiku は決定的でないため、2 往復とも問い合わせる方式は採れない。封に引数のハッシュを含めるのは、安い見積書を高い依頼に付け替える細工を防ぐため。`buildPaymentRequirements` は呼び出し側の `extra` を合成して残すので（`...parsedPrice.extra, ...resourceConfig.extra`）、封を載せる余地がある。買い手は `accepted` をそのまま返す（`x402-payer.ts` で確認）ため、封は無傷で往復する。保存先を持たせる案（引数のハッシュから見積額を引く表）はステートレス（決定22）を崩すうえ資源が増えるため採らない。有効期限を 15 分と長めに取るのは、決定31・48 の人の承認で買い手が待つ経路があり、短いと承認の間に失効して買えなくなるため。**留意**: 買い手が `accepted` を組み直す実装だと封が落ちる。`x402-payer.ts` には ProcessPayment が完全な PaymentPayload を返した場合にそれをそのまま使う分岐があり、その経路に入ると封の保存は AgentCore 側の挙動次第になる。2026-09-20 の U10 実測では内側の署名ペイロードのみが返り、この分岐には入っていない **実装（2026-09-21）**: `server/src/pricing/quote-seal.ts` と `quote.ts`。区切り文字に `.` を使うと価格の小数点で分割が狂う不具合を、テストが先に捕まえた（区切りは縦棒に変更）。鍵は `seal-key.ts` が環境変数か Secrets Manager から調達し、どちらも無ければ開発用の鍵に落ちて警告を残す。CDK では値をテンプレートに出さないため自動生成させ、関数には ARN だけを渡す **改定（2026-09-21、ユーザー判断）**: 署名（HMAC）と鍵を取りやめ、見積書は平文（版・段・価格を縦棒で連ねた形）で往復させる。ライブラリの都合で必須なのは「同じ額を 2 往復で再現すること」だけで、署名が防ぐのは安い見積書を高い依頼に付け替える細工に限られる。ところが決定56 では段が価格と生成規模の両方を決めるため、買い手が段を下げても安くて小さいページが返るだけで、等級を選び直したのと変わらない。実測でも重い依頼に梅の規模を指示したときの出力は 6,256 トークン（原価 $0.09 程度）で、梅の価格 $0.1 とほぼ釣り合う。お試し実装で Secrets Manager と鍵管理を抱える利得がないため外した。代わりに、読んだ見積書の価格が今の価格表と食い違えば使わない（表を差し替えた直後の古い見積書を弾く）。実運用で段ごとの粗利差を大きく取るなら署名を足す |
| 56 | 有料ツールの段と規模の決め方（決定54 の「見積もって値を付ける」を「判じて規模を制御する」に改める） | 売り手が依頼文から段（梅・竹・松）を判じ、段に対応する目安トークン数を生成時のシステムプロンプトに織り込む。目安は 梅 5,000 / 竹 8,000 / 松 12,000 トークン。価格は段で決まり、公表した価格表に従う。`maxTokens` は打ち切りが起きないよう目安の 2 倍以上を確保する（打ち切ると閉じタグ欠落の HTML が黙って返る。`generate-html.ts` のコメント参照）。買い手への提示は「ご依頼の内容ではおよそ N トークン相当の分量になるため梅（$X）です」という形で、段の根拠を添える。段を決めるのは売り手 | 2026-09-20 の実測による転換。①依頼文から実際の出力量は予測できない。実生成 20 件のラベルに対し Haiku の的中は 10/20 で、最頻クラスだけを答える基準線 9/20 とほぼ変わらない。出力量を決めているのは依頼の複雑さではなくモデルがどこまで書き込むかで、「ペットショップの動物紹介ページ」が 9,766 トークン、「社内報告用の売上グラフと表」が 5,881 トークンという具合に依頼文からは読めない ②一方、目安を指示すれば出力量は追従する。3 依頼 × 3 段の 9 点すべてで順序が保たれ、実測 3,374 の依頼が梅 4,871 / 竹 8,426 / 松 9,242、実測 13,378 の依頼が梅 6,256 / 竹 9,980 / 松 14,316 と動いた。打ち切りは 0 件。ゆえに予測をやめ、判断と制御に切り替える。目安は引き寄せる力であって固定する力ではなく、軽い依頼に松を指示しても 9,000 台で頭打ちになるが、軽い依頼を松と判ずること自体が判定の誤りなので実害はない（2026-09-20 ユーザー判断）。段の呼称に 梅 / 竹 / 松 を採るのは、大小を含意する語（軽・並・重など）だと判定がその語感に引かれるため。なお `score`（順序尺度）で判じる限り、モデルに渡るのは順序付きの説明文だけで段の名前は渡らない。価格は実測原価（Sonnet 4.6 の出力を $15/1M として 梅 ≒ $0.075 / 竹 ≒ $0.12 / 松 ≒ $0.18。Bedrock の実料金は別建て）に利幅を乗せて決める。利幅は未決 **実装（2026-09-21）**: 価格は仮置きで 梅 $0.1 / 竹 $0.15 / 松 $0.2（ユーザー判断）。段の呼称に 梅竹松 を採ったのは、大小を含意する語だと判定がその語感に引かれるため。判定器は `pricing/judge.ts` で、既定は Bedrock の Haiku だが `createSystemOneJudge` に差し替えれば Jev や互換サーバーでも動く。`maxTokens` は目安の 3 倍を確保し、打ち切りを避ける。価格表は AppConfig（Lambda 拡張経由）から読み、読めない設定は退けて直前の表を使い続ける。拡張レイヤーの ARN はリージョンとアーキテクチャごとに異なるため CDK のパラメータで受け取り、省略時は既定の表で動く **見直し（2026-09-21、TypeSafe の docs と突き合わせ）**: 判定器の点検は決定53 に記した。値付け側では `parseTierTable` が価格の順序と 0 ドルを検めていなかった穴を塞いだ（目安トークン数の単調増加だけを見ていたため、松が梅より安い表や0 ドルの表が通っていた）。併せて判定器が返す確信度を `ResolvedQuote` 経由で 402 のログに出すようにした。値付けの判断には使わない |
| 57 | 見積もり要求の乱発への防護（U12 の決着） | 判定器を呼び出し予算で包む。容量と補充速度を持つバケツで、使い切ったら判定器を呼ばず既定の段（竹）で売る。予算はコンテナごとに持ち、既定は容量 20・毎秒 0.2 回の補充。判定器へ渡す依頼文は 2,000 文字で切り詰める | 決定56 により 402 の経路が判定器の呼び出しを伴うようになり、U9 と同種の「タダで売り手に仕事をさせる」経路が増えた。予算方式を採るのは、攻撃の形（短い依頼を大量に送る、長い依頼を少量送る）に依らず単位時間あたりの費用に天井が付くため。依頼文の長さによる足切りも検討したが、実測では短い依頼が松に相当する例（「ペットショップの動物紹介ページ」で 9,766 トークン）があり、長さと段が対応しないので採らなかった。尽きたときに売り買いを止めないのは、止めると乱発する側の狙いどおりになるため。見積もりの精度は落ちるが、既定の段で売れば取引は成立する。Lambda の同時実行数（決定19 の reservedConcurrency）と併せて、全体の天井は「同時実行数 × 予算」になる |
| 58 | 段の判定器を Haiku と Jev で切り替えられるようにする（決定53 の「既定は Haiku」を保ったまま選択肢を足す） | 切り替えは AppConfig の既存 profile（`tier-table`）に `judge` の欄を足して行う。値は `bedrock`（既定）か `systemone`。再 deploy は要らず、判定器はリクエストごとに選ぶ。`judge` が無い・読めない場合は `bedrock` に戻すが、価格表そのものは巻き添えにしない。Jev の API キーは Secrets Manager に置く。CDK が作るのは目印（`REPLACE_ME`）入りの器だけで、値は人が `pnpm set:jev-key` で入れる（鍵は標準入力で受け、引数にもログにも残さない）。Lambda はコールドスタートに一度だけ読み、コンテナ内に保持する。ローカルは環境変数 `TYPESAFE_API_KEY` を優先。鍵が未投入・読めないときは Haiku に留まり警告を出す。Jev 経路は公式 SDK `@typesafe-ai/sdk` に載せ、待ちは 1 回 3 秒・再送 1 回に締める。呼び出し予算（決定57）のバケツはコンテナに 1 つで、判定器を選び直しても引き継ぐ | 2026-09-21 にユーザー依頼で着手し、本物の Jev（`jev-1.13.0`）を実測したうえで決めた。①**精度の優劣は、この評価では判定できない**。9/20 と同じ 20 件・同じ主観ラベルで測ると、出荷する 3 段の文言では Haiku 17/20（2 回とも同じ 3 件を外す）に対し Jev 14〜15/20。ただしこの文言はもともと Bedrock 用のシステムプロンプトで、9/21 に前置きを括り出して System One 側へも渡したものである（実体は Haiku のプロンプト）。梅から「画面」の語を外した文言では Jev 16〜17/20・Haiku 13/20 と逆転する（⑤）。すなわち文言は判定器ごとに効き、かつ現行の文言も差し替え候補も同じ 20 件を見ながら書かれているため、両者の数字とも出来レースである。同一条件の再測でも 14↔15・16↔17 と揺れており、1〜2 件の差は雑音の内。**それぞれの得意な文言どうしで比べれば Haiku 17 対 Jev 16〜17 で、差は認められない**と読むのが妥当。4 段の枠組みでは Jev 16/20 に対し Haiku 19/20（9/20 の記録）だが、そちらも Jev 側の文言はローカルの小型モデル向けに書いたもので、同じ偏りがある。優劣を言うには、文言の作成に使っていない事例と 20 件より多い数が要る（未実施）。②**速度は Jev が約 2 倍**（540ms 対 1,010ms）。ただし $0.1 の取引と 600 秒の処理予算に対しては誤差で、決定53 の当初所見は変わらない。③**英語 criteria に利は無い**。9/20 に「criteria を英語にすると効く」と観測したのはローカルの小型基盤の癖で、本物の Jev では再現しなかった（4 段で日本語 16 対 英語 15、3 段では回ごとに順位が入れ替わる）。ゆえに文言は日本語のまま共有する。④**分量の語彙に書き換えると両判定器とも悪化する**。定性的な分量（「まとまった分量」等）で 12/20、目安トークン数の明示で Jev 11/20・Haiku 14/20。前者は全件が中央の段へ潰れ、後者は Jev が LLM の出力量の相場観を持たないため系統的に少なく見積もる。「数量の手がかりは判定を乱す」（`judge.ts`）は小型モデル固有の癖ではなく、本物の判定器でも成り立つ。⑤**判定器ごとに最適な文言が異なる**。梅から「一つの画面で足りる」を外すと Jev は 16〜17/20 へ上がるが Haiku は 13/20 へ落ちる（逆も同様）。9/21 に置いた「両判定器の土台を揃える」という方針は、揃えること自体に性能上の根拠が無く、一方を犠牲にする。現状は既定の Haiku を最良に保つ文言を採る（この選択が、①で述べた比較の偏りの出どころでもある）。⑥**Jev は境界で揺れる**。同じ入力で回ごとに段が変わる（3 段で id 8・12・13・19）。Haiku は 2 回とも同じ結果だった。決定55 で見積書が往復するため 1 取引の中では破綻しないが、同じ依頼を二度出すと価格が変わり得る。⑦確信度は誤りの検出に使えない（id 13 を確信度 0.98 で外す）。決定53 の「確信度の閾値分岐は要らない」を裏付ける。鍵を Secrets Manager に置くのはユーザー指示。CDK の `Secret` は既定でランダムな値を生成するため、それに任せると出鱈目な鍵で毎回 401 になり全件が中央の段へ落ちる。目印を入れて「未投入」と判別できるようにしたのはこのため。切り替えると買い手の依頼文が AWS の外（`api.typesafe.ai`）へ出る（ZDR は enterprise 契約のみ。docs「Data handling」）⑧**費用**（同日の実測トークン数による）。判定 1 回あたり Jev $0.000021 / Haiku $0.00029 で、Jev が 13〜16 倍安い。公称の単価差は 24 倍（$0.042 対 $1.00 per MTok。Jev は出力無料）だが、Jev が同じ内容に 1.8 倍のトークンを使うため実差は縮む（498 対 269）。ただし判定は生成原価の 0.16〜0.39% にすぎない（生成は Sonnet 4.6 が 5,000〜12,000 トークン書き、原価 $0.075〜0.18）。判定を無償にしても原価は 0.2% しか下がらず、速度差 470ms も Lambda 時間に直せば 1 取引 $0.0000078 である。加えて Jev 経路には Secrets Manager の月額 $0.40 が付き、節約（$0.00028/取引）と釣り合うのは月 1,450 件から。Haiku の料率は Claude 第一者 API の $1.00 / $5.00 per MTok を当てた（Bedrock ap-northeast-1 の料率は価格表 API・価格 JSON・価格ページのいずれからも取得できず）。仮に 3 倍でも原価比 0.72% で結論は動かない。⑨**総括**: Jev は速く安いという事実は測って確かめた。しかしこの用途はその利点を活かせない。1 取引 $0.1〜0.2 に判定 1 回という構図では判定が原価の 0.2% しかなく、600 秒の処理予算（決定31）に対して 0.5 秒の差も効かない。精度で上回るとも言えない以上、既定を替える決定打が無い。ゆえに既定は Haiku に据え、切り替えは選択肢として残す。Jev の経済性が効くのは、1 件あたりの価値が小さく回数が桁違いに多い判定（ルーティング・選別・ランキング等）であって、本サンプルの構図ではない。なお本測定が覆うのは「日本語の依頼文から 3 段の規模を判ずる」という 1 つの課題・20 件のみで、他の用途に一般化できるものではない。 |

## 未決論点

検証してから決定に昇格させる。実装で触れる前に必ずここを確認すること。

| # | 論点 | 現状 | 検証方法 |
| --- | --- | --- | --- |
| U1 | TypeScript から AgentCore Payments API を呼べるか | 決着（2026-08-31）: 呼べる。`@aws-sdk/client-bedrock-agentcore` 3.1121.0 に Payments データプレーン 11 コマンド、`-control` にコントロールプレーン CRUD が完備と実測。決定24 に昇格 | - |
| U2 | X-PAYMENT ヘッダの搬送方法 | 決着（2026-08-30）: ヘッダ搬送そのものを不要化。リクエストヘッダは requestHeaderAllowlist で透過できるが、レスポンスの 402 ステータス・カスタムヘッダは返せないことが判明（aws-samples が売り手を CloudFront + Lambda@Edge に置くのはこのため）。インプロトコル方式（決定17）を採用して回避 | - |
| U3 | 認証の共有 | 決着（2026-08-30）: 論点消滅。決定19 で売り手を無認証の Lambda Function URL に置いたため、インバウンド認証そのものが無くなり、agent-app の User Pool と共有する必要も消えた。認可は x402 の支払いが単独で担う。検討過程で「AgentCore のデータプレーンは CORS 全開（allow-origin `*`、リクエストヘッダはエコーで全許可、`Mcp-Session-Id` は expose 済み）」を実測しており、決定10 のブラウザ直接取得は IAM・JWT いずれでも成立することは確認できていた | - |
| U4 | 402 応答（payment-required）の MCP 上の表現 | 決着（2026-08-30）: x402 公式モノレポの `@x402/mcp` を採用（決定17）。Cloudflare（HTTP レイヤ寄り）・Vercel x402-mcp（停滞中）・SEP-2007（Draft）と比較のうえ、公式 SDK かつインプロトコルの同パッケージが最適と判断 | - |
| U5 | ext-apps の SDK v2 対応時期 | 移植 PR（Port to SDK v2）は未完了で closed。2026-07-28 適合性違反 issue が open | 実装フェーズの節目で ext-apps のリリースを確認し、対応したら決定3を更新。**2026-09-03 再確認**: npm の最新は 1.7.5（2026-07-23 公開）で peer は sdk ^1.29 のまま。v2 対応は無く決定3 は据え置き |
| U6 | 有料ツール結果の structuredContent 搬送 | 決着（2026-08-31）: 低レベル API で回避（決定25 に昇格）。上流（@x402/mcp 2.24.0）は未修正のままで、issue 報告は③完了後に改めて判断 | - |
| U7 | 失敗時の買い手保護（返金に相当する仕組み） | 未決（2026-09-03 起票）。`exact` スキームは先払いで、決済確定後の返金はプロトコルに無い（売り手が通常の送金で返すアプリ側の運用）。x402 v2 の支払いフローは3種: `authorization`（成功後に settle。失敗なら署名の期限切れで自然に無効。x402 の基本思想）/ `upfront`（本サンプル。決定21。失敗時のリスクが買い手に移り返金手段が無い。2026-09-03 の二重支払い事故の背景）/ `escrow`（預託 → charge か cancel/refund の二段 settle。@x402/core 2.24 に `refundDeadline` / `captureDeadline` と cancel 失敗時の回収情報が実装済み。EVM 側のエスクロー契約と facilitator の対応が要る）。本サンプルでは実装に踏み込まず論点として残す（ユーザー決定）。候補: ①x402.org facilitator の escrow 対応を実測し使えれば乗り換える ②`exact` のまま売り手を冪等化し、返金ではなく成果物の再取得で救う（決定31 の残課題）③売り手保護と買い手保護の優先順位を改めて決める（決定21 の再検討） | facilitator の `/supported` で escrow 系の scheme / network が広告されるか、Base Sepolia にエスクロー契約があるかを確認する。使えるなら `paymentFlow: "escrow"` で買い手側の署名（AgentCore Payments の ProcessPayment）が通るかを実測 |
| U8 | AgentCore Payments のロール分離（決定37 の積み残し） | 未決（2026-09-05 起票）。AWS の IAM ガイドは `CreatePaymentSession`（セッションの書き込み）と `ProcessPayment` を別ロールに置くことを求めるが、共有 Lambda 1 つが両方を実行する現構成では、ロールを分けても同じ identity が双方を握るため要件を満たせない。現状は決定37 のアプリ側の判定で上限を担保している | セッション作成を Lambda から出す（人手の承認や別の実行単位に寄せる）構成が、AWS Blocks の共有 Lambda 構成で成立するかを検討する。成立するなら ManagementRole / ProcessPaymentRole に分け、決定37 のアプリ側の判定は多層防御として残す |
| U9 | 売り手側の支払い署名の検証（EIP-712 の復元） | 未決（2026-09-07 起票、同日訂正）。`@x402/mcp` 2.24 の `createPaymentWrapper` は、買い手がエコーバックする `accepted` ブロックしか照合せず、`payload.authorization` の中身（宛先・金額・有効期限・署名）は一切見ずに facilitator へ渡す（偽 facilitator で実測）。無認証の公開エンドポイント（決定19）では、誰でも売り手を経由して settle 往復を無制限に発生させられる。オンチェーンの資金は動かない（facilitator が弾く）が、Lambda の実行時間と facilitator 側の受け入れ枠を食い、正規の買い手が巻き添えを食う。**この脅威は未解消**。起票時は `server/src/payment-guard.ts`（宛先・金額・有効期間・署名の形式を settle 前に見る門番）で 「対応済みの範囲」と書いたが、同日のセルフレビューで誤りと判明し訂正した。攻撃者は無支払いの `tools/call` で 402 応答から `accepts` をタダで入手し、それを `accepted` に転記し、`authorization` を条件どおりに手書きし、署名欄に任意の 130 桁 16 進数を入れるだけで門番を通過して `/settle` に到達できる（実測）。upfront では上流が `/verify` を呼ばないため、署名が 実際に検証されるのは facilitator の `/settle` だけで、その手前の唯一のゲートが形式検査になっていた。門番が弾けるのは 「攻撃者がわざわざ選ぶ理由の無い壊れ方」だけで、位置づけは軽量な入力検証にとどまる。実際に塞ぐ手は EIP-712 署名の ローカル復元だけだが、スマートコントラクトウォレット（ERC-1271 / 6492）が署名した正当な支払いを弾く恐れがあり、本サンプルの買い手（AgentCore Payments の埋め込みウォレット）が EOA かスマートアカウントかは未確認。ユーザー決定（2026-09-07）: 門番は軽量な入力検証として残し、主張を格下げする。復元は実測待ち | 次に実決済を通す際に、売り手のログで `authorization.from` と署名の形を記録する。EOA と確認できたら `viem` の `recoverTypedDataAddress`（`@x402/evm` の実行時依存として viem は既に同梱済み）で復元を足し、決定へ昇格させる。そのとき `x402-flow.test.ts` の「【未対応・U9】捏造ペイロード」のテストが落ちるので、期待値を「settle まで届かない」へ書き換える。スマートアカウントだった場合は復元を入れず、payer 単位のクォータ（2026-09-07 の点検で Tier 2 に置いた項目。implementation-log.md 参照）へ寄せる |
| U10 | AgentCore Payments が `upto` スキームを署名できるか | **決着（2026-09-20）: 署名できる**。`scheme: "upto"` の支払い条件（`extra.facilitatorAddress` に x402.org facilitator の署名者アドレスを入れたもの）を `ProcessPayment(CRYPTO_X402)` に渡したところ `PROOF_GENERATED` が返り、`permit2Authorization` を得た。中身は witness に `facilitator` と `to`、`permitted.amount` に提示額、`spender` に upto のプロキシ契約。ap-southeast-1 で実測。これにより決定54 の割引経路は買い手本命の経路でも成立する。併せて分かったこととして、署名の時点で PaymentSession の枠が**上限額**で消費される（枠 $0.05 に対し $0.01 の要件で残 $0.04 を実測）。決済が起きなくても戻らず、データプレーンに決済結果を通知する API も無いため、`upto` で割り引いても買い手の枠は上限額のまま減る | 決着済み。売り手に `upto` を併記する実装の後、`pnpm buy:once`（使い捨てウォレット）とクラウドの買い手の双方で往復を通すこと |
| U11 | 見積額を 402 応答と支払い呼び出しで一致させる方法 | **決着（2026-09-20）: 決定55 に昇格**。上流の照合規則（`paymentRequirementsMatchAccepted`）・`extra` の合成（`buildPaymentRequirements`）・買い手の `accepted` の返し方（`x402-payer.ts`）をいずれも実装で確認し、HMAC で封じた見積書を `extra.quote` に載せる方式で成立することを確かめた。残るのは実装時の確認で、偽 facilitator を使う `x402-flow.test.ts` に「見積額がぶれた場合」「安い見積書を高い依頼に付け替える細工」の 2 つを足す | 決着済み。テストは値付けの実装と同時に書く（先にテストだけ書いても対象が無い） |
| U12 | 見積もり要求の乱発への防護 | **決着（2026-09-21）: 決定57 に昇格**。402 を返す経路が判定器の呼び出しを伴うため、無認証の公開エンドポイント（決定19）では支払う気のない相手が見積もりだけを繰り返せる。呼び出し予算と依頼文の切り詰めで、単位時間あたりの費用に天井を付けた | 決着済み。実地では CloudWatch で「予算を使い切りました」の警告の頻度を見る |
